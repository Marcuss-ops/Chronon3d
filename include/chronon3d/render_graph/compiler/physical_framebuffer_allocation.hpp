#pragma once

// Compatibility include for build targets that still include the historical
// path. The allocation plan is now CompiledResourceTable.
#include <chronon3d/render_graph/compiler/compiled_resource_table.hpp>
