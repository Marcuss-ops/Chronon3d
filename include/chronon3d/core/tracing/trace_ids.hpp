#pragma once

// ============================================================================
// trace_ids.hpp — Universal trace correlation IDs (placeholder)
//
// Reserved for the Perfetto backend migration.  This file will host the
// canonical ID vocabulary used to correlate events across threads and
// hardware engines (job_id, frame_id, graph_instance_id, stable_node_id,
// physical_slot, surface_handle, decode_slot, ...) once the backend lands.
//
// Intentionally empty today: no new SDK symbol is added until the
// backend that consumes it exists (AGENTS.md — no unnecessary API
// expansion in include/chronon3d/).
// ============================================================================
