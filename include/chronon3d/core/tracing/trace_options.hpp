#pragma once

// ============================================================================
// trace_options.hpp — Perfetto trace-session options (placeholder)
//
// Reserved for the Perfetto backend migration (see tracing.hpp header
// comment).  This file will host the job-scoped TraceOptions struct
// (enabled, output path, TraceLevel, buffer size, warmup) once the
// TraceSession backend lands.
//
// Intentionally empty today: no new SDK symbol is added until the
// backend that consumes it exists (AGENTS.md — no unnecessary API
// expansion in include/chronon3d/).
// ============================================================================
