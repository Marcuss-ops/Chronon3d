#include<chronon3d/presets/motion_renderer.hpp>#include<chronon3d/presets/motion_resolver.hpp>#include<chronon3d/registry/shape_ids.hpp>#include<chronon3d/math/camera_pose.hpp>#include<algorithm>#include<cmath>#include<utility>#include<glm/gtc/matrix_inverse.hpp>namespacechronon3d::presets::motion{namespace{std::stringapply_text_reveal(conststd::string&text,f32reveal){if(text.empty()){return{};}constf32clamped=std::clamp(reveal,0.0f,1.0f);constsize_tcount=static_cast<size_t>(std::ceil(static_cast<f32>(text.size())*clamped));returntext.substr(0,std::min(count,text.size()));}MotionStatecompose_state(constMotionState&parent,constMotionState&child){MotionStateout=child;out.visible=parent.visible&&child.visible;out.position=parent.position+child.position;out.scale={parent.scale.x*child.scale.x,parent.scale.y*child.scale.y,parent.scale.z*child.scale.z};out.rotation=parent.rotation+child.rotation;out.opacity=parent.opacity*child.opacity;out.blur=std::max(parent.blur,child.blur);if(parent.effects.glow_enabled&&!child.effects.glow_enabled){out.effects.glow_enabled=true;out.effects.glow=parent.effects.glow;}if(parent.effects.shadow_enabled&&!child.effects.shadow_enabled){out.effects.shadow_enabled=true;out.effects.shadow=parent.effects.shadow;}if(parent.effects.bloom_enabled&&!child.effects.bloom_enabled){out.effects.bloom_enabled=true;out.effects.bloom=parent.effects.bloom;}returnout;}std::stringprefix_name(conststd::string&prefix,conststd::string&id){if(prefix.empty()){returnid;}returnprefix+"__"+id;}voidapply_state(LayerBuilder&l,constMotionState&st,boolenable_3d){if(enable_3d){l.enable_3d();}l.position(st.position).rotate(st.rotation).scale(st.scale).opacity(st.opacity);if(st.blur>0.0f){l.blur(st.blur);}}voidapply_mask_sweep(LayerBuilder&l,constMotionObject&obj,constMotionState&st){if(st.mask_reveal>=0.999f){return;}constf32width=std::max(1.0f,obj.size_value.x);constf32height=std::max(1.0f,obj.size_value.y);constf32pad_x=std::max(96.0f,width*0.14f);constf32pad_y=std::max(40.0f,height*0.18f);constf32sweep_width=interpolate(st.mask_reveal,0.0f,1.0f,pad_x*1.2f,width+pad_x*1.2f,Easing::OutCubic);constf32x=-width*0.5f+sweep_width*0.5f-pad_x*0.6f;l.mask_rect(RectMaskParams{.size={sweep_width,height+pad_y*2.0f},.pos={x,0.0f,0.0f},.inverted=false,});}Vec3face_camera_rotation(constSceneBuilder&s,constMotionState&st,boolface_camera){if(!face_camera){return{0.0f,0.0f,0.0f};}constauto&cam=s.camera_2_5d();constVec3to_cam=cam.position-st.position;if(glm::dot(to_cam,to_cam)<1e-6f){return{0.0f,0.0f,0.0f};}// Build a billboard that faces the actual camera position.// This also handles point-of-interest cameras, where rotation_euler() is zero.constMat4billboard_view=glm::lookAtLH(st.position,cam.position,Vec3{0.0f,1.0f,0.0f});constQuatbillboard_rot=glm::quat_cast(glm::mat3(glm::inverse(billboard_view)));Vec3euler=math::camera_rotation_euler(billboard_rot);euler.y+=180.0f;returneuler;}voiddraw_content(LayerBuilder&l,constMotionObject&obj,constMotionState&st,conststd::string&layer_name){switch(obj.type){caseMotionObjectType::Text:l.shape(chronon3d::registry::shape_ids::Text,layer_name+"_text",chronon3d::TextSpec{
    .content = {.value = apply_text_reveal(obj.text_value, st.text_reveal)},
    .font = {.font_path = obj.text_style.font_path, .font_family = obj.text_style.font_family, .font_weight = obj.text_style.font_weight, .font_style = obj.text_style.font_style, .font_size = obj.text_style.font_size},
    .layout = {.box = obj.size_value, .align = obj.text_style.align == TextAlign::Left
                ? chronon3d::TextAlign::Left
                : obj.text_style.align == TextAlign::Right
                    ? chronon3d::TextAlign::Right
                    : chronon3d::TextAlign::Center, .vertical_align = obj.text_style.vertical_align, .line_height = obj.text_style.line_height, .tracking = obj.text_style.tracking, .auto_fit = false, .max_lines = 0, .overflow = TextOverflow::Clip, .wrap = TextWrap::Word},
    .appearance = {.color = obj.color_value},
    .position = {0.0f, 0.0f, 0.0f}
});break;caseMotionObjectType::Image:l.image(layer_name+"_image",{.path=obj.image_path_value,.size=obj.size_value,.pos={0.0f,0.0f,0.0f},.opacity=1.0f,});break;caseMotionObjectType::Video:l.video(obj.video_source_value);l.video_size(obj.size_value);break;caseMotionObjectType::Stock:l.rect(layer_name+"_stock_placeholder",{.size=obj.size_value,.color=obj.color_value.with_alpha(1.0f),.pos={0.0f,0.0f,0.0f},});break;caseMotionObjectType::Rect:l.rect(layer_name+"_rect",{.size=obj.size_value,.color=obj.color_value.with_alpha(1.0f),.pos={0.0f,0.0f,0.0f},});break;caseMotionObjectType::RoundedRect:l.rounded_rect(layer_name+"_rounded_rect",{.size=obj.size_value,.radius=obj.radius_value,.color=obj.color_value.with_alpha(1.0f),.pos={0.0f,0.0f,0.0f},});break;caseMotionObjectType::Circle:l.circle(layer_name+"_circle",{.radius=obj.size_value.x*0.5f,.color=obj.color_value.with_alpha(1.0f),.pos={0.0f,0.0f,0.0f},});break;caseMotionObjectType::Line:l.line(layer_name+"_line",{.from=obj.line_from_value,.to=obj.line_to_value,.thickness=obj.line_thickness_value,.color=obj.color_value.with_alpha(1.0f),});break;caseMotionObjectType::Group:break;}}voiddraw_motion_object_impl(SceneBuilder&s,constFrameContext&ctx,constMotionObject&obj,constMotionState&inherited,boolinherited_3d,conststd::string&prefix){MotionStatest=compose_state(inherited,resolve_motion_state(ctx,obj));constboolenable_3d=inherited_3d||obj.motion3d.enabled;if(!st.visible||st.opacity<=0.0f){return;}if(obj.type==MotionObjectType::Group){conststd::stringchild_prefix=prefix_name(prefix,obj.id);for(constauto&child:obj.children){draw_motion_object_impl(s,ctx,child,st,enable_3d,child_prefix);}return;}conststd::stringlayer_name=prefix_name(prefix,obj.id);constVec3face_cam_rot=face_camera_rotation(s,st,obj.motion3d.face_camera);s.layer(layer_name,[obj,st,layer_name,enable_3d,face_cam_rot](LayerBuilder&l){apply_state(l,st,enable_3d);if(obj.motion3d.face_camera){l.rotate(st.rotation+face_cam_rot);}l.opacity(st.opacity*obj.color_value.a);if(obj.preset_value==MotionPreset::MaskSweep){apply_mask_sweep(l,obj,st);}draw_content(l,obj,st,layer_name);l.blend(obj.style.blend);l.cache_static(obj.style.cache_static);if(st.effects.glow_enabled){// Bridge: copy the full GlowParams from the preset/MotionState so// layers/spread/softness/threshold/quality/etc. all flow through.// We use with_glow() instead of glow() to avoid the double-glow// routing — with_glow sets node->glow directly (which the// text/image/shape processors check), while glow() would also// push a LightGlow effect into the layer's effect stack.Glownode_glow=st.effects.glow;node_glow.enabled=true;l.with_glow(node_glow);}if(st.effects.shadow_enabled){l.drop_shadow(st.effects.shadow.offset,st.effects.shadow.color,st.effects.shadow.radius);}if(st.effects.bloom_enabled){l.bloom(st.effects.bloom.threshold,st.effects.bloom.radius,st.effects.bloom.intensity);}});}}// namespacevoiddraw_motion_object(SceneBuilder&s,constFrameContext&ctx,constMotionObject&obj){draw_motion_object_impl(s,ctx,obj,MotionState{},false,"");}voiddraw_motion_objects(SceneBuilder&s,constFrameContext&ctx,conststd::vector<MotionObject>&objects){for(constauto&obj:objects){draw_motion_object(s,ctx,obj);}}}// namespace chronon3d::presets::motion