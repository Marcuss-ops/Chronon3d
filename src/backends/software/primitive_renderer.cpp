// primitive_renderer.cpp — no standalone definitions needed.
// downsample_fb() lives in render_graph/pipeline/helpers.hpp (graph namespace).
// This file exists only so the CMake target compiles without error.
