# Test 18 Weekly Founder Dashboard — 2026 Week 28

## §Honesty Cert — `PARTIAL-VPS-LIMITED-DRY-RUN-CAPTURED`

This entry is a **structural placeholder** captured on a VPS that does not qualify
as a "working build host" for the founder weekly-scorecard cadence:

| Precondition named in user spec        | State on capture VPS                                  |
| -------------------------------------- | ----------------------------------------------------- |
| `chronon3d_cli` binary in PATH         | **MISSING**                                           |
| `~/.chronon3d/telemetry/telemetry.sqlite` | **MISSING** (directory absent)                     |
| `~/.chronon3d/telemetry/touchpoints.jsonl` | **MISSING**                                       |
| `~/.chronon3d/selftest_log/`           | **MISSING**                                           |
| system `sqlite3` binary                | **MISSING** (`TOOL_MISSING sqlite3` from inventory)   |
| system `awk` / `date` / `sort` / `grep` / `tee` | present                                       |
| `bash -n tools/run_weekly_scorecard.sh`| **SCRIPT_SYNTAX OK** (lint passes, deps precheck fails at runtime) |

The aggregator script's dependency-precondition check (Step 2 of
`run_weekly_scorecard.sh`) exited 2 with the `GATE_FAIL_INTERNAL` marker recorded
verbatim in §Raw Script Output below. The 8-metric dashboard was **NOT** computed;
the values in §Metrics Snapshot below are template placeholders representing the
failure mode, **NOT real numbers from a populated baseline**.

`§honesty` certification: `PARTIAL-on-populated-DB` (prior) **is NOT** closed yet
by this capture — the new disclosure is `PARTIAL-VPS-LIMITED-DRY-RUN-CAPTURED`.
Week-over-week delta for narrative line 7 (metrica migliorata) stays locked until
a real working build host runs the command with populated telemetry.

## §Raw Script Output (verbatim capture, 2026-07-12)

The literal output of

```
WEEKLY_COST_HOURLY_RATE=0.05 bash tools/run_weekly_scorecard.sh > docs/product-tests/TEST-18-2026-W28.md 2>&1
```

on the capture VPS:

```
GATE_FAIL_INTERNAL: run_weekly_scorecard: sqlite3 not in PATH
```

`CAPTURE_EXIT_CODE=2` (per Cat-4 ancillary gate convention: `GATE_FAIL_INTERNAL` → exit 2).

The 1-line output above is the **only** content the script produced before
fail-fast abort. The §Metrics Snapshot, §Narrative Greps, and §Cross-links sections
below are template scaffolding that frame the verbatim capture with audit context;
they are **NOT derived from any captured metric data** — every value is a
placeholder token.

## §Metrics Snapshot — 8 metrics instrumented, all PRE-COMPUTED PLACEHOLDERS

| # | Metric                          | Value (W28)        | Source                                                  | Note                                                              |
|---|---------------------------------|--------------------|---------------------------------------------------------|-------------------------------------------------------------------|
| 1 | `videos_completed`              | `[VPS-LIMITED]`    | sqlite `renders` count over 7d window                   | sqlite3 missing → cannot query                                   |
| 2 | `failure_rate`                  | `[VPS-LIMITED]`    | sqlite failed/total render ratio over 7d                | sqlite3 missing → cannot query                                   |
| 3 | `manual_touches_per_video`      | `[VPS-LIMITED]`    | touchpoints JSONL line-count ÷ sqlite render count     | JSONL does not exist + sqlite3 missing                           |
| 4 | `cost_per_finished_minute`      | `[VPS-LIMITED]`    | `(hours × WEEKLY_COST_HOURLY_RATE) ÷ total_minutes`     | `WEEKLY_COST_HOURLY_RATE=0.05` was set; no data to divide by      |
| 5 | `p95_render_time`               | `[VPS-LIMITED]`    | sqlite `render_ms` percentile 95 over 7d                | sqlite3 missing → cannot query                                   |
| 6 | `peak_memory`                   | `[VPS-LIMITED]`    | sqlite max(counter_value) over 7d                       | sqlite3 missing → cannot query                                   |
| 7 | `deterministic_hash_failures`   | `[VPS-LIMITED]`    | grep `GATE_FAIL` count in `~/.chronon3d/selftest_log/`  | log dir does not exist                                            |
| 8 | `bbox_contract_violations`      | `[VPS-LIMITED]`    | sqlite `bbox_contract_violations` counter over 7d       | sqlite3 missing → cannot query                                   |

ALL 8 metric values are `[VPS-LIMITED]` placeholders pending the first run on a
true working build host where `chronon3d_cli` + populated telemetry baseline can
be queried. **No invented numbers.** Per AGENTS.md §honesty: "non inventare numeri" /
"observable telemetry sources only".

## §Narrative Greps — 7 lines for the weekly founder reading

1. **Quanti video**: `[VPS-LIMITED]` — `videos_completed` could not be queried
   (sqlite3 binary missing on capture VPS — `GATE_FAIL_INTERNAL: ... sqlite3 not in PATH`).
2. **Costo**: `[VPS-LIMITED]` — `cost_per_finished_minute` requires populated
   telemetry to compute the divisor; only `WEEKLY_COST_HOURLY_RATE=0.05` was settable.
3. **Job falliti**: `[VPS-LIMITED]` — `failure_rate` could not be queried
   (sqlite3 missing + DB missing).
4. **Interventi manuali**: `[VPS-LIMITED]` — `manual_touches_per_video` requires
   populated `touchpoints.jsonl` + populated sqlite; neither available.
5. **Codice eliminato**: `[forward-point: docs/FEATURE_SUNSET.md]` — Test 16 cycle
   documented 5 DEFERRED-VERIFY entries (see `TICKET-INFRA-F2-DIVERGENCE` lineage),
   no actual eliminations this cycle.
6. **Bug più grave**: `[none this cycle]` — Test 17 comparison documented no P0
   regressions; Cat-5 4-doc stack stable across 4 prior cycles (Test 16 + Test 17 +
   TICKET-125 aggregator + first Test 18 commit). Test architecture (cf.
   `docs/CAMERA_AE_GAP_VENDETTA.md`) remains stable on the
   `main@7eb5c2ba`-derived baseline.
7. **Metrica migliorata**: `[n/a — baseline not yet seeded]` — week-over-week delta
   requires **≥ 2 weeks** of populated data; this entry is the structural
   placeholder. **The delta was NOT unlocked by this capture** — narrative line 7
   stays locked until a populated W++ capture exists.

## §Audit Trail

- Repository: `/home/pierone/Pyt/Chronon3d` on `main`
- Capture commit pre-state: `HEAD = d9ab9989` (Test 18 first-cycle commit, the
  aggregator + dashboard template).
- This entry will be added to the **next** commit on `main` (the PARTIAL dry-run
  cycle); see the §Last Updated footer below.
- Push state: **LOCAL-ONLY** per `TICKET-INFRA-F2-DIVERGENCE (P0)` §Open Blocker.
  The push stays deferred to F2-drain; the §honesty cert + push-blockers are
  documented inline in the commit body and CHANGELOG §Recently Closed.

## §Cross-links

- Dashboard template: [`docs/product-tests/TEST-18-WEEKLY-DASHBOARD.md`](TEST-18-WEEKLY-DASHBOARD.md)
- Aggregator script: [`tools/run_weekly_scorecard.sh`](../../tools/run_weekly_scorecard.sh)
- Open blocker ticket (Test 18): [`docs/tickets/TICKET-128-test-18-long-form-content.md`](../tickets/TICKET-128-test-18-long-form-content.md) §Stato: `OPEN` P1; §honesty: `PARTIAL-VPS-LIMITED-DRY-RUN-CAPTURED` (this capture)
- Aggregator ticket: [`docs/tickets/TICKET-125-test-aggregator.md` row 18](../tickets/TICKET-125-test-aggregator.md)
- Honest baseline spec: [`docs/baselines/main-7eb5c2ba-baseline.md`](../baselines/main-7eb5c2ba-baseline.md)
- F2 push blocker: `TICKET-INFRA-F2-DIVERGENCE (P0)` §Open Blockers in `docs/FOLLOWUP_TICKETS.md`

## §Last Updated

`2026-07-12` — **first cycle placeholder entry**; `PARTIAL-VPS-LIMITED-DRY-RUN-CAPTURED`
(VPS lacks system `sqlite3` binary + `chronon3d_cli` + populated telemetry).
Refresh on next weekly cadence cycle from a working build host — when a real
week's data is captured, this file is **regenerated** (overwritten) by re-running
the literal command with populated preconditions; the `[VPS-LIMITED]` markers are
replaced with real numbers and §Raw Script Output transitions from
`GATE_FAIL_INTERNAL` to `GATE_PASS` + populated metric table.
