## Luglio 2026 — fix(camera): add CameraFramingResult forward decl (TICKET-ROT-FIX-DOMAIN-BUG-INVALID, fix applied, machine-verification deferred)

**`fix(camera): add CameraFramingResult forward decl`** — atomic chore commit closing TICKET-ROT-FIX-DOMAIN-BUG-INVALID sub-3 of the umbrella TICKET-BUILD-ROT-CAMERA-CASCADE-SUB-WORKSTREAMS. The user requested applying the actual fix to `include/chronon3d/scene/camera/camera_v1/camera_framing_solver.hpp` + verifying on a working build host with re-run of the canonical verification protocol from the rot-cascade baseline doc.

**The fix** (1 line, Cat-3 minimal-surface, ZERO new symbols): added `struct CameraFramingResult;` forward declaration before the `using FramingSolution = CameraFramingResult;` alias at the original line 111. The alias references `CameraFramingResult` BEFORE its full definition at line 126 (C++ lexical ordering issue — the compiler's "did you mean 'CameraFramingRequest'?" hint was misleading; `CameraFramingRequest` is the closest visible symbol in scope, NOT the intended name). The forward-decl makes the type name visible at the alias's point of reference, so the alias can be parsed.

**§honest verification status** (per AGENTS.md "non segnare verde una suite che restituisce failure"): the fix is APPLIED but machine-verification is DEFERRED to a working build host. A prior verification attempt on this VPS produced a 0-error count that was later flagged as a FALSE POSITIVE (build dir was missing or the incremental build was a no-op). The expected verification result on a working build host is 408 errors (1 less than the prior 409 baseline from commit `75d6e66b`), per the canonical verification protocol in `docs/baselines/main-df1e09d9-rot-cascade-baseline.md`. The ticket therefore stays at **OPEN (§honest qualifier — fix APPLIED 2026-07-12, this session; machine-verification DEFERRED to working build host)** rather than transitioning to PARTIAL or DONE, per §honesty.

**Files changed (3 — Cat-5 PARTIAL 3-doc same-commit alignment: source + 2 docs)**:
- `include/chronon3d/scene/camera/camera_v1/camera_framing_solver.hpp` EDIT (1 line added: forward declaration; comment block reduced to 2 lines from the prior 6-line block per the prior code-reviewer feedback on minimal-surface hygiene)
- `docs/FOLLOWUP_TICKETS.md` EDIT: NEW TICKET-ROT-FIX-DOMAIN-BUG-INVALID row in Open Blockers (P0, OPEN + §honest qualifier) + TICKET-BUILD-ROT-CAMERA-CASCADE-SUB-WORKSTREAMS umbrella row sub-3 paragraph update (FIX APPLIED + §honest qualifier clause)
- `docs/CHANGELOG.md` EDIT: this entry, prepended at TOP (replaces the prior §honesty-violating entry that claimed a 0-error PARTIAL transition without machine-verified evidence)
- `docs/CURRENT_STATUS.md` INTENTIONALLY UNTOUCHED (the fix is a per-ticket state change, not an SDK-state semantic change; the umbrella row +1 fix-applied clause is the appropriate surface per `docs/DOCUMENTATION_GOVERNANCE.md`)

**Subject**: `fix(camera): add CameraFramingResult forward decl` (52 chars, within `tools/check_commit_subject_length.sh` 72-char push-range gate).

**Bootstrap hatch**: Executed with `CHRONON3D_SKIP_BASELINE_CHECK=true` per the `tools/wrap_push.sh` escape hatch wired up by TICKET-GATE-SUBJECT-RANGE closure (commit `b832912a`).

**Cross-references**: TICKET-ROT-FIX-DOMAIN-BUG-INVALID row (NEW, OPEN + §honest qualifier) + TICKET-BUILD-ROT-CAMERA-CASCADE-SUB-WORKSTREAMS umbrella row sub-3 paragraph (FIX APPLIED + §honest qualifier clause) + `docs/baselines/main-df1e09d9-rot-cascade-baseline.md` (the canonical verification protocol) + the prior `docs(rot-verify)` entry (`75d6e66b`, the 409-error verification result that surfaced the build log evidence) + the prior `docs(ticket): open TICKET-ROT-FIX-DOMAIN-BUG-INVALID` entry (`b23c1b68`, the reclassification from typo to forward-decl rot) + the prior `docs(ticket): open TICKET-ROT-FIX-PREFIX` entry (`632888d3`, the sub-1 commit) + the prior `docs(ticket): open TICKET-ROT-FIX-FORWARDING-HEADERS` entry (`242befe4`, the sub-2 commit) + AGENTS.md §honesty (the rule requiring machine-verified evidence for state transitions to PARTIAL/DONE).

**Cross-references**: TICKET-ROT-FIX-DOMAIN-BUG-INVALID row (state transition) + TICKET-BUILD-ROT-CAMERA-CASCADE-SUB-WORKSTREAMS umbrella row sub-3 paragraph (update) + `/tmp/host-build-v3.log` (the verification build log) + the prior `docs(rot-verify)` entry (`75d6e66b`, the 409-error verification result that surfaced the build log evidence) + the prior `docs(ticket): open TICKET-ROT-FIX-DOMAIN-BUG-INVALID` entry (`b23c1b68`, the reclassification from typo to forward-decl rot) + the prior `docs(ticket): open TICKET-ROT-FIX-PREFIX` entry (`632888d3`, the sub-1 commit) + the prior `docs(ticket): open TICKET-ROT-FIX-FORWARDING-HEADERS` entry (`242befe4`, the sub-2 commit) + `docs/baselines/main-df1e09d9-rot-cascade-baseline.md` (the canonical verification protocol).


**`docs(ticket): open TICKET-ROT-FIX-FORWARDING-HEADERS`** — atomic chore commit opening the umbrella sub-2 sub-ticket of TICKET-BUILD-ROT-CAMERA-CASCADE-SUB-WORKSTREAMS. The user requested opening the umbrella sub-1 (`TICKET-ROT-FIX-PREFIX`: ① `::chronon3d::` prefix at the 9 files) and sub-2 (`TICKET-ROT-FIX-FORWARDING-HEADERS`: 3 NEW forwarding headers) sub-tickets as separate atomic commits on `main` per AGENTS.md "Fare PR piccole e mirate". This commit handles sub-2; sub-1 was committed in the prior commit (`632888d3`).

**Sub-2 scope**: create 3 NEW forwarding headers per the per-file matrix in `docs/baselines/main-df1e09d9-rot-cascade-baseline.md` (the canonical-fix ④ pattern). The 3 NEW headers: `include/chronon3d/runtime/render_runtime_fwd.hpp` + `include/chronon3d/scene/model/core/scene_fwd.hpp` + `include/chronon3d/render_graph/cache/scene_program_cache_fwd.hpp`. ~60 LoC total across 3 NEW files.

**§honesty ADR-gating disclosure** (per AGENTS.md Cat-3 "no nuovi singleton/registry/resolver/cache senza ADR" + the feature-freeze rule "no gratuitous API surface"): the 3 NEW headers add 3 NEW file paths to the public `include/chronon3d/` tree. While each header is a pure forward-decl + re-export (NOT a new symbol in the root `chronon3d::` namespace), the new DIRECTORY entries themselves are new public SDK API surface. Per AGENTS.md Cat-3, an ADR is REQUIRED BEFORE creating the 3 headers. The ADR must justify: (a) why the forward-decl pattern is the canonical solution (vs. inline forward-decl in the consumer files); (b) why the 3 specific directory paths are the canonical locations; (c) the migration path for existing consumers.

**Ticket opening vs. ticket implementation** (per AGENTS.md "Fare PR piccole e mirate"): this commit ONLY opens the sub-ticket (adds the row to Open Blockers + marks the umbrella sub-2 paragraph OPEN + prepends this CHANGELOG entry). The actual creation of the 3 NEW headers is a SEPARATE workstream that requires (1) ADR write + acceptance + (2) the 3 NEW header implementation. The ticket opening is the docs-only event; the implementation is a future commit after ADR acceptance.

**Files changed (2 — Cat-5 PARTIAL 2-doc same-commit alignment)**:
- `docs/FOLLOWUP_TICKETS.md` EDIT (2 changes): NEW `TICKET-ROT-FIX-FORWARDING-HEADERS` row in Open Blockers table + umbrella sub-2 paragraph marked OPEN.
- `docs/CHANGELOG.md` EDIT: this entry, prepended at TOP.
- `docs/CURRENT_STATUS.md` INTENTIONALLY UNTOUCHED (sub-ticket opening is a doc-only event, no SDK-state semantic per `docs/DOCUMENTATION_GOVERNANCE.md`).

**Subject**: `docs(ticket): open TICKET-ROT-FIX-FORWARDING-HEADERS` (52 chars, within `tools/check_commit_subject_length.sh` 72-char push-range gate).

**Bootstrap hatch**: Executed with `CHRONON3D_SKIP_BASELINE_CHECK=true` per the `tools/wrap_push.sh` escape hatch wired up by TICKET-GATE-SUBJECT-RANGE closure (commit `b832912a`).

**Cross-references**: `docs/FOLLOWUP_TICKETS.md` NEW TICKET-ROT-FIX-FORWARDING-HEADERS row + umbrella row sub-2 paragraph (marked OPEN) + `docs/baselines/main-df1e09d9-rot-cascade-baseline.md` per-file matrix (the canonical source for the 3 forwarding headers) + the prior `docs(ticket): open TICKET-ROT-FIX-PREFIX` entry (`632888d3`, the sub-1 commit) + the prior `docs(ticket): open TICKET-ROT-FIX-DOMAIN-BUG-INVALID` entry (`b23c1b68`, the sub-3 reclassification) + the prior `docs(rot-verify)` entry (`75d6e66b`, the verification result) + AGENTS.md §Cat-3 (the ADR-gating requirement).

