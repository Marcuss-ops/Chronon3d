# Follow-up Tickets — Open Blockers Index

> Stato corrente: [`docs/CURRENT_STATUS.md`](docs/CURRENT_STATUS.md).
> Dettaglio completo di ogni ticket: [`docs/tickets/TICKET-NNN.md`](docs/tickets/).
> Cronologia completa ticket chiusi + deferred: [`docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md`](docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md).

## Open blockers (prioritari per baseline verde)

Solo ticket realmente aperti (PLANNED / PARTIAL). Nessun DONE in questa sezione.

| ID | Pri | Area | Stato | Blocca | Scheda |
|---|---|---|---|---|---|
| TICKET-036 | P0 | camera architecture gate | PLANNED | arch-boundary gate 5/6 | [TICKET-036.md](tickets/TICKET-036.md) |
| TICKET-011 | P0 | mainline build rot | PARTIAL | arch-boundary gate 1–8 (cron3d_core_tests rot multi-file eterogeneo, non solo claim iniziale).  **M1.5#7 progress (commit `2db652fb`):** OOM su `stb_image.h` single-header RISOLTO (3-layer fix: STBI_NO_* defines + per-file -O0 SKIP_PCH SKIP_UNITY + aggancio a clang/Docker come fallback documentato).  Primo wave rot emerso post-OOM-fix RISOLTO: (a) `s_text_cache` scope spostato dentro namespace anonimo in `test_draw_text_run_scratch_state.cpp`; (b) `UNITY_BUILD OFF` global per test executable in `cmake/Chronon3DTestSuite.cmake::chronon3d_add_test_suite` (risolve ODR su `fixture_exists`/`skip_if_missing`/`inter_bold`/`make_*_shape_for_test` helpers in 6+ test file); (c) `graph::Result::has_value()` aggiunto per matchare `chronon3d::Result` API surface.  **Secondo wave rot, post-UNITY_BUILD_OFF, da chiudere atomicamente:** (i) `test_text_run_umbrella_contract.cpp` — `FontLayoutIdentity.font_size` field-rename rot (commit `1b44e521` M1.5#4 ha rinominato `font_path`→`resolved_path`, `font_family`→`family`, `font_weight`→`weight`, `font_style`→`style`, `font_size`→`size`; test ancora usa nomi vecchi); (ii) `test_text_preset_descriptor.cpp:1140` — invalid `static_cast<std::string>→bool` su `CHECK_FALSE(d.fixture)` (sembra `d.fixture` è `std::string`, non `std::optional<X>` come nel resto del corpus); (iii) ulteriori rot di terzo/quarto wave attesi con piena build clean.  Pre-existing (locked dalla baseline) rot include `text_unit_map.cpp` + `test_text_material.cpp` linkage rot osservati in M1.5#5 doc-sync.  Plan: sub-ticket per file + atomic commit per singola responsabilità. | [TICKET-011.md](tickets/TICKET-011.md) |
| TICKET-005 | P1 | post-cascade cleanup | PARTIAL | arch-completeness gate 5 | [TICKET-005.md](tickets/TICKET-005.md) |
| TICKET-044 | P1 | selftest hardcoded paths | PLANNED | arch-boundary gate 5 | [TICKET-044.md](tickets/TICKET-044.md) |
| TICKET-046 | P1 | filename drift stale references | PLANNED | arch-boundary gate 5 | [TICKET-046.md](tickets/TICKET-046.md) |
| TICKET-051 | P1 | per-preset visual diagnostic | PLANNED | A4.3 visual gate | [TICKET-051.md](tickets/TICKET-051.md) |
| TICKET-080 | P1 | `install_consumer_test.sh` — vcpkg toolchain path relativa al worktree (`<wt>/vcpkg_bootstrap/...`); regressione `efd841f0` con `VCPKG_ROOT` non impostata (`regression_type: infra-setup`) | PLANNED | gate #10 (env precond) | [TICKET-080.md](tickets/TICKET-080.md) |
| TICKET-064 | P1 | ExecutionScope error model (ScopeError/ScopeErrorCode) | PARTIAL | arch-boundary gate 5 | [TICKET-064.md](tickets/TICKET-064.md) |
| TICKET-120 | P1 | C9 — 24 fallimenti pre-esistenti in `chronon3d_scene_tests` (incl. `TICKET-034D` CameraDescriptor fingerprint-serializable SIGABRT in `test_composition_default_camera.cpp:69`; `TICKET-035` anamorphic_squeeze wrong assertion in `test_camera_projection_contract.cpp:572` `CHECK(2666.67 == Approx(2250))`); emersi dopo C9 abilita build clean con `SKIP_UNITY_BUILD_INCLUSION` su `timed_text_document.cpp` + `boundary_resolver/text_unit_map.cpp` (ODR TU-locali pre-esistenti) — **Cat-1 progress: 4/24 chiusi** (commit `7ee302bf` 3 Agent3-introduced test-compile regression FocalPx/FrameRate/CameraSession + commit `4edfda5c` size()→points().size() in `camera_program_compiler.cpp:331` + commit `8b29a5bf` Cat-1 seed + commit `5985224c` Unity build deconflict: 8 file renames (compile_or_die → file-scoped unique names in 5 files; kEps → kEpsXxx in 3 files) + commit `7232722f` TICKET-035 anamorphic_squeeze fix (ratio 2.0 → 3.011 = 1.506 * 2.0; spherical fallback switched to Overscan mode); **17/24 test failure rimangono** post-build-fix (6 tests now passing che erano masked dalla build rot + 1 TICKET-035 fix = 7 net progress) | PARTIAL | certificazione Camera V1 end-to-end | [TICKET-120.md](tickets/TICKET-120.md) |
| TICKET-GATE-7-R1 | P1 | `src/runtime/**` non documentato in `CURRENT_STATUS.md` + `ROADMAP.md` | PLANNED | gate #7 doc-sync | — |
| TICKET-GATE-4-LEAK | P1 | abs-path leaks tracked in non-gitignored files: (1) `reports/perf/main-73a2aa9b-gates.json` (origin ticket); (2) `docs/tickets/TICKET-GATE-10-PHASE-4-BLACK-FU4.md:75` (`cd /home/pierone/Pyt/Chronon3d`) — leaked 2026-07-04 post-§3.1 origin/main FF-pull onto commit `35cb1127`+`2895bd88`; both files registered as fail-by-G4 case in `tools/check_gitignored_dirs.sh` | PARTIAL | gate #4 gitignored-dirs | — |
| TICKET-GATE-10-PHASE-4-FIX | P0 | `install_consumer_test.sh` Phase 4 — `ninja subcommand failure during compilation of highway_*_kernels.cpp.o` in `chronon3d_backend_software` target (carry-over rot da `9ecb4879`; failure-mode shift da Phase 4 PNG near-black a `c73f7493`); diagnostica preliminare: SKIP_UNITY_BUILD_INCLUSION side-effect da `cc48f3ee` FASE 2.2 catch2→doctest o vcpkg dep coverage gap | PARTIAL | gate #10 install_consumer (revoca feature freeze) | — |
| TICKET-RUNTIME-ADAPTER-INCOMPLETE-TYPE | P1 | pre-existing build rot in `src/backends/software/runtime_adapter.cpp` (line ~X): `std::unique_ptr<chronon3d::TextRenderResources>` (or similar field) without `#include <chronon3d/backends/text/text_render_resources.hpp>`; GCC 15 reports `error: invalid application of 'sizeof' to incomplete type 'chronon3d::TextRenderResources'` at `/usr/include/c++/15/bits/unique_ptr.h:90`. Blocks `cmake --build --target chronon3d_sdk_impl` at the C++ compilation step BEFORE the archive step — surfacing as a ninja `subcommand failed` error. Unrelated to the TICKET-GATE-10-AR-RACE-MITIGATION PRE_LINK `sync` (commit `140dc919`); the sync mitigation is correctly wired (cmake re-configure emits the TICKET-GATE-10-AR-RACE-MITIGATION message) but never fires because the build stops prematurely. Fix: add the missing include to `runtime_adapter.cpp`; TICKET-120 lineage carry-over (one of the 24 pre-existing test failures from `chronon3d_scene_tests` / `chronon3d_backend_software` may have masked this earlier). Discovered while verifying commit `140dc919`. | **Build fix: DONE (commit `5320eb29`)** / **Certifier (install_consumer_test.sh end-to-end): PARTIAL** — pending CI re-run su `main` (no baseline macchina-verificata post-fix; pre-fix baseline `aaf70032` 10/11 PASS, post-fix deve essere 11/11 per revocare feature freeze) | gate #10 install_consumer (certifier step — build step libero post-5320eb29) | — |
| TICKET-GATE-4-LEAK-NEW-FU5 | P1 | abs-path leak discovered 2026-07-04 in `docs/tickets/TICKET-GATE-10-PHASE-4-BLACK-FU4.md:75` (`cd /home/pierone/Pyt/Chronon3d`) — surfaced by `tools/check_gitignored_dirs.sh` post-§3.1 origin/main FF-pull onto `2895bd88`; propagates TICKET-GATE-4-LEAK cluster (origin `reports/perf/main-73a2aa9b-gates.json` leak). Fix: replace `/home/pierone/Pyt/Chronon3d` with `<REPO_ROOT>` relative reference (`$(git rev-parse --show-toplevel)`) in the offending ticket line 75. AGENTS.md v0.1 Cat-1 (build-corrective doc-hygiene). | PARTIAL | gate #4 gitignored-dirs (subsumed in TICKET-GATE-4-LEAK) | — |
| TICKET-GATE-10-AR-RACE-FOLLOWUP | P1 | open root-cause for `tools/sdk/check_archive_canaries.sh` post-nm `ar t` returning 0 entries in-script but 335 entries in isolation (same archive, same `ar t` command, different output). The canary correctly surfaces the discrepancy (it does NOT mask it). Investigation: bash `set -euo pipefail` + bsubscript + GATE_TMP exhaustion on multi-GB archives; isolation tests show `ar t $archive > file 2>&1` and `ar t $archive > file 2>/dev/null` both produce 335 lines standalone; the in-script misfire may be related to: (a) fd inheritance from `nm -C` reading the same archive, (b) `set -e` exit on a deferred condition, (c) shell variable substitution at the wrong call site. The canary entry `ar_race|arch:ar_t_post_nm_non_empty|always|chronon3d_sdk_impl` (commit `be8bf6cf`) is in place to detect the failure mode; the root cause itself is OPEN. **Diagnostic instrumentation added** in section (b.5) of `tools/sdk/check_archive_canaries.sh`: pre/post byte count + ar exit code + stderr size (always-on, cheap) + standalone control in /tmp (runs ONLY when primary check fails, to disambiguate $GATE_TMP-specific pathologies from binutils/ar-level issues). Remove the instrumentation once the root cause is identified and fixed. | PLANNED | gate #10 install_consumer (debug time) | — |
| TICKET-GATE-11-PRINTF-FIX | P0 | (recently closed per `ae3f02ec`; vedi sezione `Recently closed`) | DONE | risolto via commit `ae3f02ec` (`fix(software): gate #11 — drop diagnostic fprintf from GridBackgroundProcessor`); audit §3.5 grep `\\bprintf\\b\|\\bfprintf\\b` su `src/` returns ZERO hit; `tools/check_backend_sanitization.py` PASS exit 0 verificato a HEAD `4abf6954`; ticket PLANNED era stale doc-tracking (claim origin `b62ef4429` introdusse diagnostic fprintf poi droppato da `ae3f02ec`) |

Ordinamento: priorità gate-impact desc (P0 > P1), poi per ID.
## Agent3 — Compiler, Session, Errori, Legacy Cleanup (PLANNED, cluster)

Workstream design-FROZEN 2026-07-04 contro feature-freeze attivo; **nessun codice toccato**, solo ticket-schema. Origini: prompt operativo Agent3 per `camera_v1` compiler/session/cache + framerate propagation + LookAtLayer diagnostic + ADR. Ogni gate-DoD è testabile da `tests/scene/camera/test_camera_program_compiled.cpp` + `tests/runtime/test_camera_session.cpp` post-freeze. Matrice DoD (gate → ticket):

| DoD gate | Requisito | Ticket |
|---|---|---|
| (a) | `RegisteredMotionRef` non eredita metadata dal preset referenziato | TICKET-A3-METADATA |
| (b) | `DampedFollowConstraint` impone sempre `RequiresHistory` | TICKET-A3-DAMPED-HISTORY |
| (c) | `CameraFailurePolicy::KeepLastValidCamera` davvero usa l'ultima camera valida | TICKET-A3-SESSION-POLICY |
| (d) | un'evaluation fallita NON aggiorna la session cache | TICKET-A3-CACHE-LEASE |
| (e) | `CameraEvalContext::at()` non ha più framerate hardcoded | TICKET-A3-CTX-FRAMERATE |
| (f) | `pre-roll` riceve frame rate esplicito | TICKET-A3-PRE-ROLL-FPS |
| (g) | `LookAtLayer` senza `transforms` → diagnostic, non silenzio | TICKET-A3-LOOKAT-DIAGNOSTIC |
| (h) | ADR legacy deletion ha ticket concreti collegati al codice | TICKET-A3-ADR-013 (ponte verso [ADR-011](./adr/ADR-011-camera-legacy-deletion.md)) |

| TICKET-M1.5#9-SEQUENCE | P1 | progressive emptying of legacy `software_text_processor.cpp` + `rasterize_text_to_bl_image` pipeline (5 strict steps) | PLANNED | follow-up series verso la chiusura dell'era legacy Text-shape rendering. Step 1 (1/5) DONE in working tree (commit pending this session): migrate `tests/install_consumer/main.cpp` line 115-130 from `c3d::TextShape ts; ts.text = ...` a `text_node.shape.set_type(c3d::ShapeType::TextRun) + text_node.shape.text_run_shape_handle().value = std::make_shared<c3d::TextRunShape>()`. Steps pianificati: (2) migrate `src/scene/model/render_node_factory.cpp::RenderNodeFactory::text()` to internally delegate a `materialize_text_run_shape()` (preserves public signature; payload diventa `ShapeType::TextRun` + `TextRunShapeHandle`; update test callers in `tests/scene/rendering/test_render_node_factory.cpp:105` + `tests/presets/test_presets.cpp`); (3) drop `create_text_processor()` factory + forward-decl + `registry.register_shape(ShapeType::Text, ...)` call in `src/backends/software/processors/builtin_processors.cpp`; (4) delete `src/backends/software/processors/text/` directory tree (7 file: software_text_processor.cpp + software_text_effects.cpp + text_glow.cpp + text_shadow.cpp + text_processor_helpers.hpp + software_text_effects.hpp + text_effects.hpp) + drop `SOFTWARE_TEXT_PROCESSOR_SOURCES` da CMakeLists; (5) delete legacy rasterizer infrastructure (`include/chronon3d/backends/text/text_rasterizer_utils.hpp` + `src/backends/text/text_rasterizer_render.cpp` + `text_rasterizer_cache.cpp` + `text_rasterizer_ink.cpp`) + migrate/delete `tests/text/test_text_material.cpp` + `tests/text/test_text_cache_key.cpp` (i 2 file usano direttamente `rasterize_text_to_bl_image` API). Zero new public API surface aggiunto attraverso i 5 step (il legacy rasterizer è AGENTS.md Cat-3 internal-only; l'ABI non si espande). |

| ID | Pri | Area | Stato | Note (scope residuo post-freeze) |
|---|---|---|---|---|
| TICKET-A3-METADATA        | P1 | `camera_v1::compile_camera` — rimuovere `return` anticipato dopo graft del preset-referenziato; late-rebuild `failure_policy_/time_dependent_/evaluation_dependency_` | PLANNED | header OK (`CameraCompileError::CircularCatalogReference` esiste già); body `src/scene/camera/camera_v1/camera_program_compiler.cpp` ricompilazione post-graft ancora da fare |
| TICKET-A3-SESSION-POLICY  | P1 | `CameraFailurePolicy::KeepLastValidCamera` realmente recupera `CameraSession::last_valid_camera` (oggi segue il ramo di `Stop`) | PLANNED | field `last_valid_camera` già presente in `camera_session.hpp`; decision-table in body ancora cablata su Stop |
| TICKET-A3-CACHE-LEASE     | P1 | (recently closed per `a8414705` / §3.1 execution-plan; vedi sezione `Recently closed`) | DONE | chiuso via commit `a8414705c4bfd16a87efca15c31c511fd9ff8d1d` (`fix(camera): make CameraSessionLease rollback real`); working-copy-on-Entry pattern con `Entry::working_session` (CameraSession value field, sibling di `Entry::checkpoint.session`); uncommitted leases implicitly rollback both session state AND last_evaluated_frame |
| TICKET-A3-CTX-FRAMERATE   | P1 | `CameraEvalContext::at(Frame, FrameRate)` + `CameraMotionContext::at(Frame, FrameRate)` audit call-site per residui `FrameRate{30,1}` hardcoded | PLANNED | factory canonica già presente in `camera_motion_context.hpp`; audit call-site per default-nascosti |
| TICKET-A3-PRE-ROLL-FPS    | P1 | `preroll_session_for_frame(...)` riceve `FrameRate` esplicito, senza default | PLANNED | signature già parametrizzata su `frame_rate` in `camera_session_cache.hpp`; verificare che ogni call-site passi fps non-default |
| TICKET-A3-DAMPED-HISTORY  | P1 | `DampedFollowConstraint` forza sempre `ProgramKind::RequiresHistory` nel compiler | PLANNED | policy dichiarata in `camera_constraint.hpp`; compilatore deve applicarla in metadata classification |
| TICKET-A3-LOOKAT-DIAGNOSTIC | P1 | `LookAtLayer` senza `transforms` (`CameraEvalContext::transforms == nullptr`) emette `CameraDiagnostic{Code::MissingTransforms, Severity::Warning}`, no silenzio | PLANNED | `CameraEvalContext::transforms{nullptr}` esiste; emissione strutturata da aggiungere al path di evaluation |
| TICKET-A3-ADR-013         | P1 | ADR-013 (camera_v1 semantics + framerate explicit + cache-lease contract) + ticket-to-code chain verso [ADR-011](./adr/ADR-011-camera-legacy-deletion.md) | PLANNED | doc-only |

**Overlap / distinzioni importanti** (leggere prima di lavorare):

- I ticket A3-* sono **semantic-level** (compiler/session/cache contract), **non** il **type-level deletion** di [ADR-011](./adr/ADR-011-camera-legacy-deletion.md) Decisions 1–5 (AnimatedCamera2_5D / dual CameraRig / CameraShotProfile / camera_descriptor_adapters / Camera2_5D::projection_mode field). Quest'ultimo è parzialmente chiuso (`projection_mode` già rimosso in `camera_2_5d.hpp`).
- `TICKET-camera-policy-pre-existing` (rot pre-esistente in `src/render_graph/pipeline/camera_change_policy.cpp:24`) resta fuori scope del cluster A3 e si appoggia alla migration list di ADR-011 Decision 5.
- `P1 #10 Frame rate hardcoded` (DONE `6df9b429`+`560750e3`) ha coperto gli overload `render_scene` globali; `TICKET-A3-CTX-FRAMERATE` è scope narrower (factory `camera_v1::*Context::at()`).

**Avvio rigido (per AGENTS.md "avvio rigido: prima mini-sequenza 1→2→3→4"):**
`A3-METADATA → A3-SESSION-POLICY → A3-CACHE-LEASE → A3-CTX-FRAMERATE → A3-PRE-ROLL-FPS → A3-DAMPED-HISTORY → A3-LOOKAT-DIAGNOSTIC`, `A3-ADR-013 last`.

**Vincoli architetturali di esecuzione:**

- un commit per ticket; **direttamente su `main`**, no branch;
- `tools/wrap_push.sh origin main` prima di ogni push (GATE-MNT-01);
- aggiornare `CURRENT_STATUS.md` + `FOLLOWUP_TICKETS.md` nello stesso commit (gate #7 `check_doc_sync.sh`);
- `tools/check_camera_architecture.sh` deve restare 6/6 PASS dopo ogni commit;
- nessun **nuovo** singleton/registry/resolver/cache/service-locator (regola permanente).

## Partial closures (lavoro iniziato, non ancora DONE)

| ID | Pri | Area | Stato | Dettaglio |
|---|---|---|---|---|
| TICKET-070 | P1 | SoftwareBackendServices validation | PARTIAL | Validazione dipendenze backend incompleta |
| TICKET-P1-PART2 | P1 | P1 census #7–#12 (6 issues) | PARTIAL | #10 e #12 DONE; #7, #8, #9, #11 PLANNED |
| TICKET-PUBLIC-MANIFEST-03 | P1 | OPP-cpp incomplete-type in `software_backend.cpp` | PARTIAL | Fix (a) applicato in `ac5f7125`; OPP compile end-to-end ancora da verificare |

## P1 backlog (post-baseline, PLANNED)

| ID | Pri | Area | Stato | Scheda |
|---|---|---|---|---|
| TICKET-P1-07 | P1 | Asset resolver globale | PLANNED | [TICKET-P1-07.md](tickets/TICKET-P1-07.md) |
| TICKET-P1-08 | P1 | Text renderer monolite | PLANNED | [TICKET-P1-08.md](tickets/TICKET-P1-08.md) |
| TICKET-M1.5#7-FULL-SPLIT | P1 | text backend monolithic header split (M1.5#7 follow-up) | PLANNED | follow-up a M1.5#7 canonical-ownership commit: split include/chronon3d/backends/text/text_render_resources.hpp (485 LOC monolitico) nei 6 sub-header della spec utente sotto include/chronon3d/backends/text/resources/{text_render_resources,blend2d_font_cache,freetype_face_cache,glyph_atlas,raster_cache,text_scratch_pool}.hpp; contestualmente migrare text_rasterizer_render.cpp + ~30 callers da API namespace-level (glyph_atlas_lookup(), BLFontFaceCache::get_face(), lookup_text_cache/store_text_cache namespace-level) a API instance-method per-session su SoftwareSessionResources.text_resources fields; anche rinominare BLFontFaceCache→Blend2DFontCache + GlyphOutlineBuilder→FreeTypeOutlineBuilder + TextScratchManager→TextScratchPool per strict spec. Prereq arrivato con M1.5#7 commit: SoftwareSessionResources.text_resources pointer cablato. AGENTS.md Cat-1/Cat-3/Cat-5 freeze-compliant. [tickets/TICKET-M1.5#7-FULL-SPLIT.md](tickets/TICKET-M1.5#7-FULL-SPLIT.md) |
| TICKET-M1.5#7-AUDIT | P1 | forward-decl + `std::unique_ptr<X>` incomplete-type pattern audit (M1.5#7 + dual `TextRenderResources` fix carry-over) | PLANNED | follow-up a M1.5#7 + 5320eb29 + M1.5#8: scansionare l'intero codebase per il pattern `std::unique_ptr<X>` con `X` forward-declared in header ma full-include mancante nel .cpp TU. Causalità osservata: GCC 15 `error: invalid application of 'sizeof' to incomplete type` su default deleter instantiation in `/usr/include/c++/15/bits/unique_ptr.h:90`. Casi noti (entrambi chiusi): (a) `src/backends/software/runtime_adapter.cpp` (commit `5320eb29`); (b) `include/chronon3d/backends/software/software_session_resources.hpp` (M1.5#8 carry-back, incluso in commit `c9415e5b`). Plan: nuovo tool `tools/audit_incomplete_type_pattern.sh` (grep `std::unique_ptr<\w+>` + grep forward-decl `class \w+;`/`struct \w+;` + cross-ref per missing full-include nel .cpp TU) + 1 ticket per ogni caso rilevato. Output atteso: lista esaustiva di tutti i siti dove il pattern esiste attualmente o è stato storicamente rotto. AGENTS.md Cat-1/Cat-3 freeze-compliant (preventive build correction; doc-only ticket at inception, code changes per caso rilevato saranno atomic-commit singolo ticket). [tickets/TICKET-M1.5#7-AUDIT.md](tickets/TICKET-M1.5#7-AUDIT.md) |
| TICKET-M1.5#8-PRE-EXISTING-ROT | P1 | `chronon3d_core_tests` LINK rot pre-esistente (5+ test file) + 1 env-var doc gap — surfaces post-M1.5#8 | PLANNED | follow-up a M1.5#8: nuovi golden test commit rende evidente rot pre-esistente in `chronon3d_core_tests` (verificato a pristine HEAD via `git stash` test questo commit stesso — NON introdotto da M1.5#8). Sub-rot inventariati: (1) `tests/registry/test_text_preset_descriptor.cpp:210` `register_preset()` chiamato su const `TextPresetRegistry` (const-discard); (2) `tests/text/test_text_run_umbrella_contract.cpp:88` field `font_size` mancante da `FontLayoutIdentity` (post-M1.5#4 rename rot); (3) `tests/text/test_text_material.cpp:27` namespace `chronon3d::runtime::RenderRuntime` non trovato (post-Fase B2 rimozione singleton rot); (4) `tests/text/test_text_material.cpp:52,291` `FontEngine{resolver}` brace-init no candidate; (5) `tests/test_text_preset_registry.cpp:75` + `tests/support/layer_builder_inspection.hpp:75` reference `chronon3d::registry::TextAnimatorSpec` — type esiste come `chronon3d::TextAnimatorSpec` (non nel namespace registry). CONFERMATO «pre-existing rot»: tutti questi file NON sono stati modificati da M1.5#8 commit. Doc gap secondario: env-var `CHRONON3D_FORCE_NO_FRIBIDI` (introdotto in `src/backends/text/bidi_segmenter.cpp` da M1.5#8) non documentato in CURRENT_STATUS / RELEASE_GATE / ARCHITECTURE_AUDIT. Plan: split in 6 sotto-ticket atomic per file + 1 doc-alignment ticket. AGENTS.md Cat-1/Cat-3 freeze-compliant. [tickets/TICKET-M1.5#8-PRE-EXISTING-ROT.md](tickets/TICKET-M1.5#8-PRE-EXISTING-ROT.md) |
| TICKET-M1.5#6-TIGHTENING | P1 | M1.5#6 carry-over tightening-pass (4 nits surfaced at code-reviewer) | PLANNED | follow-up a M1.5#6 commit: (1) `detail::bucket_radius_for_tier` in public header `<chronon3d/backends/software/text_run_processor.hpp>` ha ancora threshold ladder `r≤0/4/8/16→0/2/7/13/20` indipendente da `text_run_stages::kBlurTierRadii`. Fold in un singolo source of truth (probabilmente via `inline constexpr` table + index-based lookup). (2) `kMaxPooledSurfaces = 8` rationale (`5 main tiers + final + shadow + crossfade slack`) va documentato inline accanto alla costante (al momento è solo nella doc del test). (3) `tests/backends/software/test_text_run_processor_bench_serial_vs_parallel.cpp` TEST_CASE "bench pool reuse across multiple bench iterations" costruisce un `TextRenderResources` fresh per call — il test name è misleading (misura FNV-1a determinismo, NON pool reuse); o rinominare o hoistare uno `TextRenderResources` esterno per genuine pool-reuse lock. (4) `src/backends/software/processors/text_run/text_run_processor/raster.cpp` fa `build_blur_tiers` ×2 BEFORE check `s.silent_success_empty` — O(G) wasted work per off-canvas text; move il short-circuit prima della pre-classify call (o early-return in `prepare.cpp` stesso). AGENTS.md Cat-3 freeze-compliant (internal). |
| TICKET-P1-09 | P1 | RenderRuntime service locator | PLANNED | [TICKET-P1-09.md](tickets/TICKET-P1-09.md) |
| TICKET-P1-11 | P1 | Timeline percorsi multipli | PLANNED | [TICKET-P1-11.md](tickets/TICKET-P1-11.md) |
| TICKET-render-pipeline-fps-defaults-audit | P1 | audit `render_scene` overloads + free-fun `float fps` params | PLANNED | code-review hygiene, non-blocking |
| TICKET-GOLDEN-10 | P1 | `golden_emoji_fallback_basic` — M5 color emoji/fallback (post-M4) | PLANNED | [ADR-014 Decision 2](adr/ADR-014-text-golden-coverage.md#decision-2--5-forward-only-tickets-for-the-spec-subset-blocked-by-feature-freeze-v01) |
| TICKET-GOLDEN-16 | P1 | `golden_anim_character_offset_wave` — Blocco 2 Character Offset pre-shaping | PLANNED | [ADR-014 Decision 2](adr/ADR-014-text-golden-coverage.md#decision-2--5-forward-only-tickets-for-the-spec-subset-blocked-by-feature-freeze-v01) |
| TICKET-GOLDEN-30 | P1 | `golden_karaoke_word_highlight` — M4 subtitle word timing | PLANNED | [ADR-014 Decision 2](adr/ADR-014-text-golden-coverage.md#decision-2--5-forward-only-tickets-for-the-spec-subset-blocked-by-feature-freeze-v01) |
| TICKET-GOLDEN-31 | P1 | `golden_character_depth_stack` — M5 per-character 3D + Expressions V2 stable | PLANNED | [ADR-014 Decision 2](adr/ADR-014-text-golden-coverage.md#decision-2--5-forward-only-tickets-for-the-spec-subset-blocked-by-feature-freeze-v01) |
| TICKET-GOLDEN-32 | P1 | `golden_text_on_arc_path` — post-TXT-09 + Blocco 11 motion blur | PLANNED | [ADR-014 Decision 2](adr/ADR-014-text-golden-coverage.md#decision-2--5-forward-only-tickets-for-the-spec-subset-blocked-by-feature-freeze-v01) |

| TICKET-M1.5#10-SEQUENCE | P1 | progressive deletion of `include/chronon3d/text/rich_text.hpp` (legacy polyfill pipeline) — 4 strict steps | PARTIAL | follow-up series verso la chiusura dell'era legacy RichTextLine/RichTextRun/draw_rich_text(): Step 1 (1/4) DONE in working tree (commit pending this session): drop obsolete `TEST_CASE("RichTextLine measures a mixed inline line with text, spacing and symbol")` da `tests/layout/test_design_kit.cpp` (~22 LOC). Il concetto "mixed inline line" non è riproducibile nel modello canonico (TextDocument+TextStyleSpan descrivono UNA `utf8` con override; `l.text()` + `l.star()` sono SEPARATE primitive in LayerBuilder, "decorative/anchor gestito dal percorso compilato" per spec). Steps pianificati: (2) sweep `RichTextRun` struct usages — atteso ZERO prod callers post-Step-1 (doc-only commit); (3) sweep `draw_rich_text` callsites — atteso ZERO prod callers (survey pre-M1.5#10 confermato da M1.5#5+M1.5#6+M1.5#8 lineage che hanno convertito 15+ content/showcases a `l.text(name, TextSpec)`); (4) `rm include/chronon3d/text/rich_text.hpp + src/backends/text/rich_text.cpp` (380+90 LOC) + drop `#include <chronon3d/text/rich_text.hpp>` da `design_kit.hpp` line 17 + trim commento aggregatore + sweep global grep. Zero new public API surface (canonical public surface `l.text(name, TextSpec)` + `l.text_run(name, TextRunParams)` invariato). Strict spec NO divide in nuovi file legacy; `rich_text.hpp` è già marcato P1-LEGACY-TEXT-PIPELINE e verrà eliminato completamente al termine. AGENTS.md v0.1 Cat-3 freeze-compliant. | [tickets/TICKET-M1.5#10-SEQUENCE.md](tickets/TICKET-M1.5#10-SEQUENCE.md) |

| TICKET-M1.5#13-SEQUENCE | P1 | refactor del registry preset testuali: 4-step per-category factory extraction (basic / kinetic / cinematic / social) + `std::span<const TextPresetDescriptor> builtin_text_presets()` span accessor | Step 1/4 DONE (basic = Subtitle factory extracted, span accessor added, shared internal helpers header landed) in commit pending this session; Steps 2/4 (kinetic = Reveal), 3/4 (cinematic), 4/4 (social = Emphasis + umbrella rewiring): PLANNED | 4-step factory extraction of text preset registry (`src/registry/text_preset_registry.cpp` → per-category factory TUs `text_preset_factories_basic/kinetic/cinematic/social.cpp` + 1 shared internal helpers header). Step 1 (this commit): `text_preset_internal_helpers.hpp` + `text_preset_factories_basic.cpp` + `builtin_text_presets()` span accessor + delete 4 Subtitle entry+compose bodies from registry TU + register bridge `register_text_preset_subtitle(r)` consumes factory vector. Steps 2/3/4: extract Reveal 10 entries → kinetic factory; Cinematic 4 entries → cinematic factory; Emphasis 4 entries → social factory. The factory TUs each emit `create_text_presets()` returning `std::vector<TextPresetDescriptor>`; the register helper `register_text_preset_<category>(r)` consumes the vector. AGENTS.md v0.1 Cat-5 freeze-compliant (structural refactor only; span accessor user-mandated as canonical read-only view of single registry). | [tickets/TICKET-M1.5#13-SEQUENCE.md](tickets/TICKET-M1.5#13-SEQUENCE.md) |

**P1 progress summary (2026-07-04):**
- P1 #1–#5: **DONE** (Part 1 completata, commit `0892a224`)
- P1 #10 (Frame rate hardcoded): **DONE** (commit `6df9b429` + `560750e3`)
- P1 #12 (CMake fragile): **DONE** (commit `59b2439f`)
- P1 #7, #8, #9, #11: **PLANNED** (post-baseline)

## In progress — fix pushed, pending CI verification

| ID | Area | Stato | Note |
|---|---|---|---|
| TICKET-GATE-10-AR-RACE | binutils `ar` "reason: Success" failure-mode regression lock: new canary entry `"ar_race\|arch:ar_t_post_nm_non_empty\|always\|chronon3d_sdk_impl"` in `cmake/Chronon3DCanarySymbols.cmake` + structural `arch:*` case branch + post-nm `ar t` re-run (section (b.5)) in `tools/sdk/check_archive_canaries.sh` + symmetric pre-nm `ar t` direct-write (no `\| sort`). The canary correctly fires on the current archive (`pre-ar=335, post-nm-ar=0`); root cause of the in-script `ar t` post-nm misfire is OPEN — see TICKET-GATE-10-AR-RACE-FOLLOWUP. Commit `be8bf6cf`. | FIX PUSHED | gate #10 canary lock now in place; OPEN root-cause investigation tracked separately |
| Gate-10 Phase 4 (CMake) | consumer-SDK CMake case-fix (TBB/xxHash title-case) + 44 transitive headers → `cmake/Chronon3DPublicHeaders.cmake` (commit `21b9fb5d`) | FIX PUSHED | CI machine-verified Phase 4 ancora pending |
| TICKET-GATE-10-PHASE-4-BLACK | consumer build/link regression-lock: `chronon3d::sdk::RenderEngine` canary entry in `cmake/Chronon3DCanarySymbols.cmake` (commit `8fd0588e`); substring `chronon3d::sdk::RenderEngine` matches all ctor/dtor/render/set_* demangled symbols emitted by `src/runtime/sdk_render_engine.cpp` via `chronon3d_runtime` OBJECT lib; canary gate (Phase 3.5) catches future drops of `sdk_render_engine.cpp.o` from the SDK archive BEFORE consumer attempts to link. Phase 4 itself PASS: `[BOUNDARY-OK] 230400/230400 pixels >5/255` (consumer renders white-grid background + blank text layer due to missing fonts; both confirmed via stderr diagnostics). | FIX PUSHED | Gate #10 Phase 4 PASS post force-rebuild; canary lock prevents recurrence |
| M1.5#8 | `src/text/text_resolver.cpp` (391 LOC) → 6 single-responsibility sub-cpp sotto `src/text/resolver/` (`text_run_resolver` orchestrator + `text_bidi_resolver` + `text_font_resolver` + `text_span_resolver`); **un solo servizio** `FontResolver::resolve(const FontRequest&)` come canonicale (INTERNAL, NO superficie pubblica); back-compat `resolve_fallback_fonts` ora `[[deprecated("Use internal FontResolver instead")]]` ma firma invariata (zero breaking change ai callers). `getenv("CHRONON3D_FORCE_NO_FRIBIDI")` short-circuit in `src/backends/text/bidi_segmenter.cpp` per determinismo cross-friBidi system. NEW `tests/text/test_text_font_resolver_golden.cpp` locka determinismo con FNV-1a hash su `ResolvedTextTree` (3 TEST_CASE deterministici: same-input-identical-hash, env-var override → FriBidi-independent-hash, font-fallback order-independent for equivalent requests). Nessun 2° resolver in backend/builder; AGENTS.md Cat-3/Cat-5 freeze-compliant. Carry-back 1-line include fixup `include/chronon3d/backends/software/software_session_resources.hpp` full-include `<chronon3d/backends/text/text_render_resources.hpp>` (pre-existing rot M1.5#7 surfaced da `std::unique_ptr<TextRenderResources>` sizeof-check; full-include già in software-side boundary, WP-3 invariant preserved). Pre-existing `chronon3d_core_tests` LINK rot (5 test file) tracked in `TICKET-M1.5#8-PRE-EXISTING-ROT` follow-up. Build: `chronon3d_text_core` + `chronon3d_backend_text` EXIT 0. | commit `pending this session` (M1.5#8 — text_resolver.cpp monolith split) |
| Gate-10 Phase 4 (Runtime) | `RenderPipeline::debug_graph` default-arg chain: `float fps = 0.0f` sentinel (commit `75035f2b` → `c40ba16f` post-rebase) | FIX PUSHED | Phase 4 CI end-to-end pending |
| TICKET-GATE-10-PHASE-4-FULL | 15 missing non-internal transitive headers (vendored glm/tracy/magic_enum wrappers); first hit: `detail/framebuffer_impl.hpp` | FIX PUSHED | Incremental batch-add a `cmake/Chronon3DPublicHeaders.cmake` |
| TICKET-Phase4-BlurTierRadii | `static constexpr std::array<i32, 5> kBlurTierRadii = {{0, 2, 7, 13, 20}};` ripristinato in `text_run_processor.cpp` | FIX PUSHED | Gate #10 Phase 4 |
| TICKET-GATE-10-PHASE-4 | consumer-build Phase 4: triple fix (a) cmake case-fix, (b) runtime default-arg, (c) BlurTierRadii restore | FIX PUSHED | CI confirm ancora pending |

## Recently closed (DONE — verificati, giugno–luglio 2026)

| ID | Area | Commit / Note |
|---|---|---|
| ADR-014 | text golden coverage extension — 12 user-spec text golden tests under `tests/text_golden/user_spec/` packed into existing `chronon3d_text_golden_tests` target via `target_sources(...)` + `UNITY_BUILD OFF` + `verify_golden` reuse from `tests/visual/support/golden_test.hpp`. Phase A (`484830ee`): 12 `*.cpp` skeleton stubs (doctest + `CHECK(true)` placeholder); tools/test_golden_driver.sh (bash-only, no Python); docs/adr/ADR-014-text-golden-coverage.md (locks 12-minimum + 5 forward-only scope); 5 forward-only TICKET-GOLDEN-10/16/30/31/32 rows seeded in P1 backlog. Phase B (`d62a8174`): polished scene-builder implementations restored from out-of-tree backup `/tmp/adr014_full/`; 20 PNG goldens re-captured via `CHRONON3D_UPDATE_GOLDENS=1` to gitignored runtime artifacts `test_renders/golden/text/user_spec_*.png`; Cat-1 build-rot fix in `src/text/resolver/text_span_resolver.cpp` removed duplicate `struct FontSubRange` definition re-introduced post-M1.5#8 split (commit `acb7d3d9`), resolution opted for Option B (delete upstream's incorrect NOTE pointing at `include/chronon3d/...` since `text_span_resolver.hpp` is internal to `src/text/resolver/`, contradicting AGENTS v0.1 Cat-3), file-level NOTE block correctly references the internal hpp. AGENTS v0.1 freeze Cat-1 + Cat-2 only: zero new public API surface (no new symbols in `include/chronon3d/`); zero new registry/resolver/sampler/cache/service-locator/singleton; `tools/check_doc_sync.sh` (gate #7) PASS. | commits `d62a8174` (Phase B replay atop upstream `64c947bd`) + `484830ee` (Phase A baseline) |
| TICKET-P1 | P1 census (5 issues, done): text/cache/cmake | commit `0892a224` |
| TICKET-GATE-1+9-SSOT-POSIX-REGEX | `tools/check_architecture_boundaries.sh` Check 16 — mawk-incompat `\s`/`\S` regex migrato a POSIX `[[:space:]]`/`[^[:space:]]` (3 awk blocks); riabilitava gate #1 + gate #9 Check 16 SDK public-deps SSoT wiring assertion su sistemi con `mawk` (default Debian/Ubuntu) | commit `a5ee07e7`; 2/11 flipped FAIL→PASS (g1: `[16/16] SDK public-deps SSoT wiring ... PASS (registry=7 entries; marker=1 substitution / 0 hand-written)`; g9: `architecture-boundary child_target_check_arch_boundary (12 checks) PASS`); rebased clean su origin/main (no file overlap con `cc48f3ee` + `34289560`) |
| TICKET-GATE-MNT-01-EXT | `tools/check_main_clean.sh` Step 4 + `tools/wrap_push.sh` Step 2.5 + `tools/install_consumer_test.sh` Step 0 + `AGENTS.md` §GATE-MNT-01 — per-branch rebase invariant verify + auto-repair (`branch.${TARGET_BRANCH}.rebase = true` se mancante) | commit `c73f7493`; new invariant (idempotent + forward-only); non cambia gate count ma rende il rebase invariant bloccante al gate failure; smoke-test delete→fail + restore→pass + auto-repair→pass verificato end-to-end |
| TICKET-camera-policy-pre-existing (M1.5#1 + M1.5#2 carryover) | `src/render_graph/pipeline/camera_change_policy.cpp:24` — `Camera2_5D::projection_mode` rot rimossa (sostituita con `Camera2_5D::optics_mode`); riabilitava build di `chronon3d_render_graph_tests` (M1.5#1) e `chronon3d_core_tests` (M1.5#2) | commit `ac514fea` (origin fix, projection_mode→optics_mode) + verified-clean su `main@83e74169` (`grep -rnE 'Camera2_5D::projection_mode' src/` → 0 hit + `prev->optics_mode` + `current.optics_mode` canonici in `camera_change_policy.cpp`); `tools/test_architectural.sh` + `tools/check_architecture_boundaries.sh` script-exit OK (carry-over FAIL su SDK public-deps SSoT Check 16 = pre-esistente gate-10 lineage, non introdotto da questo commit) |
| TICKET-A3-CACHE-LEASE | CameraSessionLease session-state rollback real: `Entry::working_session` (CameraSession value-field, sibling di `Entry::checkpoint.session`); `acquire()` copia `checkpoint.session` → `working_session` (vector capacity reuse = **0 allocazioni hot-path**); `commit()` copia `working_session` → `checkpoint.session`; uncommitted leases implicitly rollback both session state AND `last_evaluated_frame`. NEW `tests/runtime/test_camera_session_cache_failed_no_commit_session_state.cpp` 3 TEST_CASEs (failed+no_commit / success+commit / success+no_commit) locks the lease contract. ADR-013 Decision 3 source-anchors updated. **Design deviation** (documented in CHANGELOG): user-spec richiedeva `std::optional<CameraSession>` ON THE LEASE; implemented as `CameraSession working_session` value-field on `Entry` instead per `thinker-with-files-gemini` design review (vector capacity reuse vs per-acquire heap alloc). `tools/check_camera_architecture.sh` 6/6 PASS. `tools/wrap_push.sh origin main` GATE-MNT-01 PASS. AGENTS.md v0.1 Cat-1 + Cat-2 + Cat-3 freeze-compliant. | commit `a8414705` |
|
| TICKET-P1-10 | Frame rate hardcoded | commit `6df9b429` + `560750e3` |
| TICKET-P1-12 | CMake fragile (ar merge + include_private) | commit `59b2439f` |
| M1.5#1 | TextRunNode.cpp orchestratore + 3 helpers (`text_run/`) + return-channel test | commit `82d2b0e0` |
| M1.5#2 | text_run_driver.cpp orchestratore + 3 helpers (`src/text/driver/`) + `EffectiveTextState` | commit `e837e274` |
| M1.5#3 | text_run.hpp umbrella split into 5 sub-headers (`text_layout_identity` / `text_run_layout` / `text_layout_cache` / `text_run_shape` / `text_run_hash`); Fase B3 deprecated singleton symbols (`shared_text_layout_cache` / `reset_shared_text_layout_cache`) rimossi da public API surface; lock contract test `tests/text/test_text_run_umbrella_contract.cpp` (static_assert-driven, cat-2 freeze-compliant, no Blend2D / no font engine dependency) | commit `843dc863` (refactor) + this-commit (lock test + cmake entry + doc sync) |
| M1.5#4 Commit A | text_run.cpp (363 LOC) — estrazione `hash_text_run_shape` family (single-arg + Frame-overload) nel single-responsibility `src/text/text_run_hash.cpp` (verbatim body move, no behavioural changes); wired nel `chronon3d_text_core` OBJECT library; doc sync nello stesso commit; reviewer APPROVE FOR COMMIT (3 minor non-blocking, saranno coperti in Commit B/C). Chronon3d_text_core build verde post-estrazione. | commit `b85863f1` (M1.5#3 lock) + this-commit |
| M1.5#4 (full) | text_run.cpp (363 LOC) split into 3 sub-files: `text_run_hash.cpp` (~155 LOC: layout_hash + shaping_hash + hash_text_run_shape), `text_layout_cache.cpp` (~145 LOC: TextLayoutCacheKey::digest + TextLayoutCache impl), `text_layout_identity.cpp` (~15 LOC: font_layout_identity_of(TextRunLayout)). `text_run.cpp` ridotto a umbrella vuoto poi rimosso da CMakeLists. `FontLayoutIdentity` canonical field rename (font_path→resolved_path, font_family→family, font_weight→weight, font_style→style, font_size→size) + `variation_axes` field. Build: `chronon3d_text_core` PASS. | commits `b85863f1`, `3a9ede42`, `fd21d76c`, `1b44e521` |
| M1.5#5 | `variation_axes` (std::string) plumbed through TextRunLayout, TextLayoutCacheKey, TextLayoutRequest + layout_hash, shaping_hash, digest(), build_cache_key, font_layout_identity_of. `operator==` is `=default` → auto-covers new field. Build: `chronon3d_text_core` PASS. | commit `7e9e17d3` |
| M1.5#6 | `compile_text_layout` refactored into canonical 7-stage pipeline. 6 file-local helpers extracted: `build_empty_paragraph_layout`, `resolve_target_paragraph`, `try_cache_lookup`, `store_in_cache`, `finalize_text_run_layout`, `effective_paragraph_style`. Pipeline: 1=VALIDATE 2=RESOLVE 3=SHAPE 4=FAILURE-POLICY 5=COMPOSE 6=FONT-SPANS 7=CACHE. Public surface unchanged. Build: `chronon3d_text_core` PASS. | commit `6cc290e5` |
| TICKET-021 | Camera V1: PoseTracksSource variance-preserving dispatch | commit `82d2b0e0` (post-sync) + §2.A lock-tests|
| TICKET-022 | Camera V1: single-application canonical-order lock (orientation + constraint) + 4-camera-trajectory-lookat compiled path covered by §4.B.3 Single-Look-At Policy + §4.B.2 Canonical-order Application | commit `82d2b0e0` (post-sync) + Step 4+5 trajectory work + §4.B.1-§4.B.3 lock-tests in `test_camera_program_compiled.cpp` |
| TICKET-024 | Camera V1: orbit position math in camera-local basis | commit `82d2b0e0` (post-sync) + §4.C lock-tests |
| TICKET-025 | Camera V1: OrientAlongPath semantic correctness (tangent/roll/keep_horizon/degenerate-tangent) | Step 4 + `test_orient_along_path.cpp` lock-tests |
| TICKET-077 | gate-3 I2: `software_renderer.hpp` LOC 223→182 | — |
| TICKET-079 | gate-3 I5: `attach_software_backend` SWRenderer& → SWRenderer* | commit `ac5f7125` |
| TICKET-078 | gate-3 I3: non-local includes 7→6 | — |
| TICKET-087 | gate-3 I3: build-fix preflight_fonts | — |
| TICKET-106 | gate-3 I5: path-list-parity regression test | — |
| TICKET-086 | thread_local cache → static + mutex | — |
| TICKET-088 | HarfBuzz buffer pool in FontEngine | — |
| TICKET-090 | draw_text_run private stages refactor | — |
| TICKET-090b | draw_text_run stage-marker comments | — |
| TICKET-100 | eliminate legacy materialize_text_run_shape | — |
| TICKET-101 | compile_text_layout (TextDocument, paragraph_index) | — |
| TICKET-092 | per-paragraph error accumulator | — |
| TICKET-103a | TextLayoutRequest direction/language/features | — |
| TICKET-104 | PendingTextRun consumed real-decrement | — |
| TICKET-105 | identity/preservation regression test suite | — |
| TICKET-107 | per-category register helpers promoted | — |
| TICKET-108a | aliases.hpp 7 typedefs adopted | — |
| TICKET-109 | TextRunBuilderInspector + migrate 53 callsites | — |
| TICKET-098 | text_preset_registry per-category helpers | — |
| TICKET-099a | internal-only domain type aliases | — |
| TICKET-077b | std::call_once elimination (6 files) | — |
| TICKET-076 | wrap_push.sh auto-FF merge before gate | — |
| TICKET-075 | GATE-MNT-01 strict-SHA → ancestor-relation | — |
| TICKET-069 | FreeTypeFaceCache destruction contract | — |
| TICKET-074 | remove dead glyph_selector_v2 pipeline | — |
| TICKET-068 | crossfade+stroke regression test | — |
| TICKET-118 | SoftwareBackend::draw_node real impl + drop dummy TextRunProcessor | — |
| TICKET-GATE-10-PHASE-4-BLACK | consumer build/link regression-lock: `chronon3d::sdk::RenderEngine` canary entry (commit `8fd0588e`) + force-rebuild of `chronon3d_sdk_impl` (deleting stale `libchronon3d_sdk_impl.a` then `cmake --build --target chronon3d_sdk_impl`) restored `sdk_render_engine.cpp.o` to the merged archive; consumer linked and rendered `[BOUNDARY-OK] 230400/230400 pixels >5/255`; canary entry prevents recurrence of stale-build-cache regression | commit `8fd0588e`; canary gate now 9 present + 1 skipped (guard OFF) + 0 missing; Phase 4 PASS end-to-end |
| TICKET-119 | SoftwareBackend m_owner back-pointer removal + ProcessorSourceExtras bridge | — |
| P0-1 | RenderGraphNode::execute() → Result<OwnedFB, NodeExecutionError> (36 file) | — |
| P0-2 | FontLayoutIdentity unificata su cache/hash/fastpath/prewarm (5 file) | — |
| TICKET-PUBLIC-MANIFEST-01 | CMake public-manifest sed-artefact repair | commit `59db2da5` |
| TICKET-PUBLIC-MANIFEST-02 | OPP-include-path cascade closure (22 OPP-internal + 1 PUBLIC + 1 moved + 1 python-test) | 2026-07-02 |
| TICKET-render-session-cpp-brace | OPP-side `render_session.cpp` missing namespace-closing `}` | 2026-07-02 |
| Gate-10 build rot | consumer SDK + install_consumer_test Phase 1-3 verde | commit `ac5f7125` |
| TICKET-067 | GATE-MNT-01 divergence fix | — |
| TICKET-096 | stash@{0} debt triage (DROP 2 retro-fixup) | — |
| TICKET-080-unicode | unicode extraction utf8_decoder + whitespace (distinto da TICKET-080 vcpkg toolchain) | — |
| P1-CMAKE-01 | CMake core/video boundary fix (P1 #5) | — |

## Deferred / non-blocking (PLANNED, post-baseline)

| ID | Area | Note |
|---|---|---|
| _(nessun carry-over residuo: TICKET-camera-policy-pre-existing promosso a Recently closed 2026-07-04)_ | | |

## Altri ticket aperti

Tutti i ticket aperti non nella top-10 (TICKET-024, TICKET-026, TICKET-066, TICKET-078-deferred, TICKET-081a–116) sono tracciati in [`docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md`](docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md). Le specifiche di accettazione complete sono nei file [`docs/tickets/`](docs/tickets/) individuali.

Cronologia completa: [`docs/CHANGELOG.md`](docs/CHANGELOG.md) e [`docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md`](docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md).
