# Follow-up Tickets — Open Blockers Index

> Stato corrente: [`docs/CURRENT_STATUS.md`](docs/CURRENT_STATUS.md).
> Dettaglio completo: [`docs/tickets/`](docs/tickets/).
> Cronologia ticket chiusi: [`docs/CHANGELOG.md`](docs/CHANGELOG.md).
> Cronologia estesa: [`docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md`](docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md).

## Open Blockers (≤10)

Solo ticket realmente aperti (PLANNED / PARTIAL / OPEN).

| TICKET-PRODUCTL-AUNCH-FAIL-LOUD-TEST-7 | P1 | Test #7 fail-loud contract — gates `exit_code != 0` + MP4 absent post-run + canonical-error-vocabulary tokens (composition|Composition, layer|Layer, asset|Asset, path|Path) in stderr + NO silent-fallback markers (`fallback frame`, `black frame`, `continue on error`, `fallback to silence`); 5 declarative JSON fixtures at `tests/fixtures/{missing_font,missing_image,corrupt_video,invalid_camera,non_writable_dir}.json` (rot-class-differentiated: 3 = AssetResolver-fail, 1 = CameraProgram-fail, 1 = OutputWrite-fail); canonical gate at `tools/check_first_principles_fail_loud.sh` (one gate, NO per-fixture scripts per AGENTS.md Cat-3 anti-duplication); orchestrator wire-in via `tools/first_principles_product_check.sh` `== Fail-loud errors ==` section EDIT replacing `# TODO (Test #7)` stub with `bash "$SCRIPT_DIR/check_first_principles_fail_loud.sh"` invocation | **WIRED (2026-07-12, atomic chore commit on main); machine-verification OPEN** — code LANDED + orchestrator wire-in DONE + 5 fixtures VALIDATED (jq syntax + schema-coherent + bash -n exit 0 on the script after the `[${token}]` regex bug-fix to `grep -qiE "\\b${token}\\b"`); dry-run on this VPS emits EXPECTED `GATE_FAIL: chronon3d_cli binary not found in standard build paths` (canonical build hint included) — VERIFIED 2026-07-12; **macchina-verifica on the 5-fixture fail-loud contract** (each fixture individually: `exit != 0` + `output_file_absent` + `stderr contains ≥3 of 4 canonical tokens` + `NO silent-fallback markers`) DEFERRED to working build host per AGENTS.md §honesty (vcpkg glm/magic_enum + tmpfs env on this VPS prevents `cmake --build` to produce the `chronon3d_cli` binary). Forward-points (NOT in this commit per AGENTS.md "Fare PR piccole e mirate"): (1) working-build-host re-bake + run `bash tools/check_first_principles_fail_loud.sh` expecting PASS once binary is present; (2) per-fixture on FAIL-loud MISS, open per-fixture fix-forward tickets; (3) cross-link to TICKET-PRODUCT-LAUNCH-DEMO §closure-lineage (same First-Principles Product Check orchestrator family, Test #1 + Test #7 same `== Product demo ==`-adjacent family). Cat-3 (zero new public SDK API surface — pure `tools/` + `tests/fixtures/` artifacts) + Cat-5 (3-doc same-commit satisfaction: CHANGELOG entry prepended + this row + CURRENT_STATUS row added in same atomic chore commit) + §honest-qualifier (machine-verification deferred to working build host per AGENTS.md §honesty "non segnare verde una suite che restituisce failure") SATISFIED. | chronon3d_cli binary machina-verification (build-host, post-vcpkg-bootstrap) |

| TICKET-SOURCE-CONFLICT-MARKERS-ROT | P0 | **10 unresolved `<<<<<<< HEAD` / `=======` / `>>>>>>> ...` git merge markers** in committed main source code (3 source: cinematic_title_helpers.hpp + tilt_sweep_title_v2.cpp + two_point_five_d_compositions.cpp, 7 test: camera_visual_compare.cpp + pr3_compositions.cpp + cinematic_motion_tests.cpp + ae_parity_tests.cpp + camera_truth_orbit.cpp + 01_rotate_z_not_cut.cpp + 04_hangul_composition.cpp) — discovered 2026-07-12 via ripgrep on main working tree | **DONE (2026-07-12, atomic chore commit on main)** | chronon3d build rot (broken markers prevent compilation) + AGENTS.md §honesty violation (main HEAD claimed PASS but rot persisted) — 10-file conflict markers resolved per the thinker's per-file resolution table (cinematic 2 EDITS combining `line_height` HEAD + `overflow` other, two_point_five_d <<< stay preserving HEAD's `.position = {0.0f, 0.0f, -1000.0f}`, 4 test files <<< stay simplifying to single `REQUIRE_FALSE(result.golden_missing)`, camera_truth_orbit EDIT combining hud_label variable (other) with .placement API (HEAD), 01_rotate_z_not_cut <<< stay (HEAD's comment), 04_hangul_composition >>>> stay fixing HEAD's C++ syntax error). NEW `tools/check_no_source_conflict_markers.sh` Cat-5 rot-preventive gate (line-start anchored scan of .cpp/.hpp/.h/.c/.cmake, excludes .py intentional selftest + .md prose). Cross-link: `docs/CHANGELOG.md` prepended fix entry + `tools/check_no_source_conflict_markers.sh` (the new gate). **Forward-point (NOT in this commit)**: wire `tools/check_no_source_conflict_markers.sh` into `tools/wrap_push.sh` Step 4.5 (after rotate/hygiene/integration gates, before commit-subject-length). Out of scope for the foundation commit per AGENTS.md "Fare PR piccole e mirate". Cat-3 (zero new public SDK API — tool-only change) + Cat-5 3-doc same-commit + §honesty SATISFIED (1 commit per-file resolution strictly per thinker's table; no silent partial fixes; gate presence documented). |
| TICKET-ROT-FIX-DOMAIN-BUG-INVALID | P0 | rot at `include/chronon3d/scene/camera/camera_v1/camera_framing_solver.hpp:111` is **C++ lexical ordering / forward-declaration visibility**, NOT a typo (reclassified from typo per AGENTS.md §honesty — the rot-cascade baseline doc's DOMAIN BUG "typo" claim is INVALID / mischaracterized because the alias `using FramingSolution = CameraFramingResult;` at line 111 references `CameraFramingResult` BEFORE its full definition at line 126; the compiler's "did you mean 'CameraFramingRequest'?" hint was misleading — `CameraFramingRequest` is the closest visible symbol in scope, NOT the intended name; aliasing `FramingSolution` to `CameraFramingRequest` would break the user-spec semantics where `FramingSolution` is the canonical name for the RESULT, not the REQUEST); fix is `struct CameraFramingResult;` forward declaration before the alias (1 line, Cat-3 minimal-surface, ZERO new symbols, ZERO API changes) | **OPEN (§honest qualifier — fix APPLIED 2026-07-12, this session; machine-verification DEFERRED to working build host per AGENTS.md §honesty "non segnare verde una suite che restituisce failure" — a prior verification attempt on this VPS produced a 0-error count that was later flagged as a FALSE POSITIVE; the expected verification result on a working build host is 408 errors (1 less than the prior 409 baseline from commit `75d6e66b`) per the canonical verification protocol in `docs/baselines/main-df1e09d9-rot-cascade-baseline.md`; 408 rot errors remain blocking full project build per the TICKET-BUILD-ROT-CASCADE-CAMERA umbrella)** | full project build (working build host) — `bash tools/check_architecture_boundaries.sh` + `cmake --build` with rot-cascade resolution; expected error count 408 (1 less than 409 baseline) per the rot-cascade baseline doc canonical verification protocol; on success transition TICKET-ROT-FIX-DOMAIN-BUG-INVALID to PARTIAL (fix verified) and continue with the remaining TICKET-BUILD-ROT-CASCADE-CAMERA umbrella |
| TICKET-BUILD-ROT-CAMERA-CASCADE-SUB-WORKSTREAMS | P0 | umbrella of TICKET-BUILD-ROT-CASCADE-CAMERA — 11-file rot-cascade fix split into 3 sub-tickets per AGENTS.md "Fare PR piccole e mirate" + Cat-3 anti-duplication (independent verification per sub-ticket; each sub-ticket = separate atomic commit on `main`) | **OPEN (umbrella; 3 sub-tickets forward-pointed as separate atomic commits; +1 per-file matrix staleness disclosure 2026-07-12 this session)** | full project build + 6 Text V1 goldens re-bake — 3 sub-tickets to be opened as separate atomic commits on main: (sub-1) `TICKET-ROT-FIX-PREFIX` — ① `::chronon3d::` prefix at 9 files (`render_runtime.hpp` + `scene.hpp` + `text/{blend2d_glyph_conversion,text_rasterizer_render,font_engine}.cpp` + `execution_scope.hpp` + `composition.hpp` + `scene_program_cache.hpp` + `render_session.hpp`) per the per-file matrix in `docs/baselines/main-df1e09d9-rot-cascade-baseline.md`; ~50 LoC; **OPEN (this session, 2026-07-12, see new row below)**; (sub-2) `TICKET-ROT-FIX-FORWARDING-HEADERS` — ④ 3 NEW forwarding headers (NEW `include/chronon3d/runtime/render_runtime_fwd.hpp` + NEW `include/chronon3d/scene/model/core/scene_fwd.hpp` + NEW `include/chronon3d/render_graph/cache/scene_program_cache_fwd.hpp`); ~60 LoC; **OPEN (this session, 2026-07-12, see new row below)**; (sub-3) `TICKET-ROT-FIX-DOMAIN-BUG-INVALID` — **FIX APPLIED (this commit, 2026-07-12) + §honest qualifier (machine-verification DEFERRED to working build host)** — **REPLACES prior typo claim** (reclassified per AGENTS.md §honesty because the rot-cascade baseline doc's DOMAIN BUG "typo" claim is INVALID / mischaracterized). The rot at `include/chronon3d/scene/camera/camera_v1/camera_framing_solver.hpp:111` is **C++ lexical ordering / forward-declaration visibility**, NOT a typo. The alias `using FramingSolution = CameraFramingResult;` at line 111 references `CameraFramingResult` BEFORE its full definition at line 126 (line 72 = `struct CameraFramingRequest`; line 111 = alias; line 126 = `struct CameraFramingResult`). The compiler's "did you mean 'CameraFramingRequest'?" hint was misleading — `CameraFramingRequest` is the closest visible symbol in scope, NOT the intended name. Aliasing `FramingSolution` to `CameraFramingRequest` would break the user-spec semantics (where `FramingSolution` is the canonical name for the RESULT, not the REQUEST). The fix (1 line, Cat-3 minimal-surface, ZERO new symbols, ZERO API changes): `struct CameraFramingResult;` forward declaration before the alias. See new `TICKET-ROT-FIX-DOMAIN-BUG-INVALID` row in Open Blockers above (state: OPEN + §honest qualifier — fix APPLIED 2026-07-12, machine-verification DEFERRED to working build host). **Total estimated fix LoC across 3 sub-tickets: ~110 LoC across 13 files** (10 file edits + 3 NEW forwarding headers). Each sub-ticket = 1 atomic commit on `main` per AGENTS.md "Fare PR piccole e mirate"; sub-tickets are NOT opened in this commit (per user spec verbatim: "Each sub-ticket should be a separate atomic commit on main"). macchina-verifica of all 3 sub-tickets deferred to working build host per AGENTS.md §honesty (vcpkg glm/magic_enum + tmpfs env on this VPS). **+1 per-file matrix staleness disclosure (2026-07-12, this session)**: the verification result above (TICKET-BUILD-ROT-CASCADE-CAMERA parent row +1 clause) shows the per-file matrix in `docs/baselines/main-df1e09d9-rot-cascade-baseline.md` is STALE — actual rot is **67% bigger** than predicted (409 vs 245). The 3 sub-tickets' file lists (sub-1: 9 files, sub-2: 3 NEW headers, sub-3: 1 file) need re-verification against the actual build log before each sub-ticket is opened. The ACTUAL top error files are: `render_runtime.hpp` 88 errors (matches) + `scene.hpp` 72 errors (3x matrix prediction) + `scene_hasher.hpp` 48 errors (4x matrix prediction). Cross-link: `docs/CHANGELOG.md` 2026-07-12 umbrella-ticket entry + TICKET-BUILD-ROT-CASCADE-CAMERA parent row (this session, +1 umbrella cross-link + +1 verification-result clause) + `docs/baselines/main-df1e09d9-rot-cascade-baseline.md` (the per-file canonical-fix decision matrix; canonical source for the 3 sub-tickets' file lists + fix patterns — STALE post-this-session-verification) + the prior `TICKET-CONTENT-TEXT-CAMERA-V1-ROT-VARIANT-MOVE` sub-ticket (commit `561c52a9`) + the parallel agent's `docs(camera-text-rot): verify upstream 21-error rot RESOLVED` (TICKET-CONTENT-TEXT-CAMERA-V1-ROT Phase 1 closure, this session). **+1 camera_v1 SDK-surface audit (2026-07-12, this session)**: machine-verified `chronon3d::chronon3d::*` double-namespace rot is **NOT present in the camera_v1 source code** — 0 hits via `grep -rE 'chronon3d::chronon3d' include/chronon3d/scene/camera src/scene/camera` across 63 .hpp/.cpp files. The per-file matrix in the baseline doc has 1 camera_v1 file (`camera_framing_solver.hpp`) — already reclassified as sub-3 forward-decl rot (TICKET-ROT-FIX-DOMAIN-BUG-INVALID, this session, see separate row). 3 camera_v1 files use the canonical `::chronon3d::` prefix correctly (`camera_motion_blur.hpp` + `camera_descriptor_adapters.cpp` + `internal/shutter_pose_sampler.cpp` — total 4 canonical-prefix usages; the per-file matrix fix ① `::chronon3d::` prefix pattern is already applied here). **§honest qualifier**: the rot-cascade is generated by the upstream parent-fix-template-instantiation cascade, NOT by camera_v1 source — the 205 `chronon3d::chronon3d` hits in the build log are cascade artifacts (`/tmp/host-build-df1e09d9-v2.log`), not source-code rot. macchina-verifica of this audit ran on this VPS (no vcpkg needed — pure `grep -rE` on source); cross-link `docs/CHANGELOG.md` 2026-07-12 `docs(audit): camera_v1 surface clean of double-namespace rot` entry (this session). **+1 sub-ticket opening — TICKET-ROT-FIX-PREFIX (2026-07-12, this session)**: see new row below. The umbrella sub-1 paragraph below is now marked OPEN; per-file matrix needs re-verification on working build host before implementation (5/9 EXIST at canonical paths + 3/9 EXIST at RENAMED paths + 1/9 truly MISSING per this-session re-verification). | |
