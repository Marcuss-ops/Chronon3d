# Follow-up Tickets — Open Blockers Index

> Snapshot: `main@88d2deec` — 2026-06-29. Linux-only.
>
> File canonico per gli aperti. Massimo ~10 righe di tabella, ordinate per blocco della
> certificazione `Baseline verde: CERTIFICATA` (vedi `AGENTS.md` regola di lavoro #4). Tutti gli
> altri ticket aperti non-baseline-critical vivono in `docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md`
> (verbatim cronologia) e in git history pre-Step-6 (commit `6f3309e6`).
>
> Schema pregresso (`🔵 Planned` / `🟡 Partial` / `🟢 Done`) rimane canonico. Estensioni:
> `🟡 Code-fix` introdotto da Step 6 redux è sinonimo di `🟡 Partial` (tickets con codice già
> scritto ma non ancora macchina-verificato). `🔵 OPEN` introdotta per TICKET-051 è sinonimo
> di `🔵 Planned` (Step 4 spec Agent 3).

## Open blockers (top 10 — prioritari per `Baseline verde`)

| ID | Area | Stato | File principali | Gate |
|---|---|---|---|---|
| TICKET-077 | gate-3 / I2 — `software_renderer.hpp` LOC overflow (233 > 200 limit, target R4) | 🔵 Planned | `include/chronon3d/backends/software/software_renderer.hpp` | check_software_renderer_boundary I2 | Header exceeds the 200-line limit set by gate-3 soft-boundary R4. At `a8842d20` the header emits `[FAIL] I2: header LOC=233 > 200`. **XREF**: still failing on `main@eecfda9c` -- the 9 commits between `a8842d20..HEAD` did not modify `software_renderer.hpp` per `git log --oneline a8842d20..HEAD -- include/chronon3d/backends/software/software_renderer.hpp` (only `software_renderer.cpp` had a touch in `e19125dd` — RenderIOFenceGuard + node.payload→shape + text_core→cache, irrelevant to LOC). **Closure path**: split into logical sub-headers (e.g., `software_renderer_types.hpp` for POD types, `software_renderer_config.hpp` for runtime config structs, leave top-level header for the class + lifecycle API) OR move private-only helpers into `src/backends/software/software_renderer.cpp` private namespace. **Acceptance**: header LOC ≤ 200 AND gate-3 I2 PASS re-run. **Forbidden-list mirror**: AGENTS.md v0.1 forbids expanding `include/chronon3d/` public API surface during feature freeze; TICKET-077 fix must NOT introduce new public classes -- only relocate existing public symbols into sub-headers already-imported by consumers. **Cross-ticket coordination**: this TICKET-077 fix MUST NOT regress I3 (TICKET-078) by splitting headers in a way that re-exports non-local includes from new sub-headers. |
| TICKET-078 | gate-3 / I3 — `software_renderer.hpp` non-local includes overflow (7 > 6 limit, target R4) | 🟢 Done | `include/chronon3d/backends/software/software_renderer.hpp` + `src/backends/software/software_renderer.cpp` | check_software_renderer_boundary I3 | Header has 7 non-local `#include <chronon3d/...>` directives (gate-3 R4 limit is 6). At `a8842d20` + `main@eecfda9c` the offending includes, in order: `renderer.hpp`, `render_settings.hpp`, `image_renderer.hpp`, `counters.hpp`, `software_render_session.hpp`, `text_render_resources.hpp`, `config.hpp`. **Closure path (ranked)**: (1) forward-declare types in the header + push actual includes into `software_renderer.cpp` where the methods reference them; (2) drop headers that the header doesn't actually need (TypeForward audit: keep only headers whose types are USED in the header body, not transitively-through-member-types); (3) consolidate related includes into a single `software_render_types.hpp` umbrella to amortize the include count. **Acceptance**: non-local include count ≤ 6 AND gate-3 I3 PASS re-run. **Cross-ticket coordination**: TICKET-077's split-into-sub-headers path would likely INTRODUCE include counters -- the I3 fix MUST come BEFORE the I2 fix to avoid a regression loop; recommended sequencing is 078 → 077 (reduce first, then split) or 077+078 single combined commit. **🟢 CLOSED** (atomic on `main` per user's "one-ticket-at-a-time" sequencing): forward-decl `struct FontPreflightSummary;` lands in the parent header (C++ rule: a function returning a value of type `T` only requires `T` complete at the call site, not the declaration site -- the .cpp carries the full include for the body's `return FontPreflightSummary{...}`); `preflight_fonts()` declaration moves out-of-class into `software_renderer.cpp` alongside its existing definition (same signature, no API change); the duplicate `text_render_resources.hpp` include inside the `#ifdef CHRONON3D_ENABLE_TEXT` block is removed (the include is moved OUTSIDE the ifdef because `FontPreflightSummary` is unconditional in `text_render_resources.hpp` -- only FreeType-gated types remain gated). Header non-local includes: 7 → 6 ✓.  Gate-3 I3 PASS re-run confirmed. **I2 (TICKET-077 follow-on commit) + I5 (TICKET-079 follow-on commit) still failing on `main` per user sequencing.** |
| TICKET-079 | gate-3 / I5 — `RuntimeAdapter::attach_software_backend` takes `SoftwareRenderer&` in process surface (target R2) | 🔵 Planned | `include/chronon3d/backends/software/runtime_adapter.hpp:57` | check_software_renderer_boundary I5 | The runtime-adapter process surface exposes `void attach_software_backend(chronon3d::SoftwareRenderer& renderer);` at `runtime_adapter.hpp:57`, which is a lvalue reference pinning `SoftwareRenderer` (the soft-boundary gate-3 R2 forbids this; only `SoftwareRenderer&&` rvalue-ref is allowed for legitimate moves). At `a8842d20` + `main@eecfda9c` this is the **only** I5 violation blocking gate-3. **Closure path (ranked)**: (1) take by `chronon3d::SoftwareBackend&` (the lower-level pipeline-facade the runtime_adapter actually wires up -- the rendering pipeline layer, not the renderer driver); (2) take by `chronon3d::runtime::RenderRuntime&` (the canonical runtime handle from `runtime/render_runtime.hpp`, already on the include list and conceptually the coupling point); (3) restructure: split `attach_software_backend` into `attach_software_backend(unique_ptr<SoftwareBackend>)` + `attach_renderer_driver(...)` so the runtime surface never names `SoftwareRenderer&`. **Acceptance**: 0 unqualified `SoftwareRenderer&` references in `src/render_graph/` + `src/runtime/` + `include/chronon3d/runtime/` + `apps/` (verified by `grep -rnE 'SoftwareRenderer&' src/backends/software/processors/ src/render_graph/ src/runtime/ include/chronon3d/render_graph/ include/chronon3d/runtime/ apps/` excluding lines containing the substring `SoftwareRenderer&&` rvalue-ref returning empty) AND gate-3 I5 PASS re-run. **API compatibility note**: option (3) is the most invasive (callers must adapt); option (1) is the most surgical (callers update `renderer` → `backend()`). |
| TICKET-087 | gate-3 / I3 — TICKET-078 closure side-effect: out-of-class `preflight_fonts` declaration invalid C++ (build broken on `linux-fast-dev` preset) | 🟢 Done | `include/chronon3d/backends/software/software_renderer.hpp` + `src/backends/software/software_renderer.cpp` + this row | check_software_renderer_boundary I3 (build-fix) | The TICKET-078 closure commit's out-of-class declaration for `SoftwareRenderer::preflight_fonts` is INVALID C++: a class member function defined at namespace scope requires a prior in-class DECLARATION; the unqualified call sites at `software_renderer.cpp:424/453` invoke in-class name lookup, which fails when the class no longer names `preflight_fonts`. The fix is to re-add the in-class declaration in the header (paired with the existing `struct FontPreflightSummary;` forward-decl -- C++ permits a function declaration with incomplete return type, so NO new `#include <text_render_resources.hpp>` is needed in the header); REMOVE the out-of-class DECLARATION block from the .cpp (keep ONLY the body definition). Gate-3 I3 STILL PASSes (6 <= 6 non-local includes preserved); header LOC delta ~+21 lines (comment trim per code-reviewer LOW recommendation; verbose 14-line block became 4 lines). AGENTS.md v0.1 Cat-1 (`Correzioni di build`) freeze-compliant: no new `#include`, no `include/chronon3d/` API surface change, signature identical to pre-TICKET-078 era. Unblocks TICKET-106 Cat-2 regression test (the previously-failing `chronon3d_core_tests` binary now compiles; ID chosen after partner commits claimed both `TICKET-086` (FASE-4 thread_local cache) and `TICKET-100` (FASE-4 legacy `materialize_text_run_shape` pipeline elimination) for unrelated refactors, so the Cat-2 test landed under the next free numeric slot). |
| TICKET-051 | A4.3 per-preset diagnostic | 🔵 OPEN | `tests/text/test_text_preset_visual.cpp` | A4.3 visual |
| TICKET-036 | chronon3d_camera_architecture_gate P0 | 🔵 Planned | `tools/check_camera_architecture.sh` | arch-boundary (gate 5/6) |
| TICKET-044 | arch_boundaries_selftest hardcoded | 🔵 Planned | `tools/check_architecture_boundaries_selftest.sh` | arch-boundary (gate 5) |
| TICKET-046 | filename drift stale refs | 🔵 Planned | `tools/check_filename_drift.sh` | arch-boundary (gate 5) |
| TICKET-011 | pre-existing mainline build rot | 🔵 Planned | `src/` | arch-boundary (gate 1–8) |
| TICKET-005 | post-cascade cleanup | 🟡 Partial | `src/` + `include/` | arch-completeness (gate 5) |
| TICKET-022 | camera double look-at compiled path | 🟡 Code-fix | `include/camera/` | arch-boundary (gate 5/6) |
| TICKET-024 | camera OrbitMotion world-Z vs camera basis | 🟡 Code-fix | `include/camera/` | camera path |
| TICKET-026 | camera MotionBlurSettings Mode duality | 🟡 Code-fix | `include/camera/` | arch-boundary (gate 5) |
| TICKET-064 | §9 ExecutionScope — ScopeError/ScopeErrorCode structured error model (PR 6.8 prep) | 🟡 Code-fix | `include/chronon3d/core/execution/scope_error.hpp` | arch-boundary (gate 5) |
| TICKET-067 | GATE-MNT-01 strict-SHA equality incompatible with post-commit push | 🟢 Done | `tools/check_main_clean.sh` | GATE-MNT-01 (pre-push wrapper). `fix(gate): swap HEAD==origin/main strict SHA for git merge-base --is-ancestor` (env-vars Agent3, atomic on `main`; 4 tracked files [`tools/check_main_clean.sh` + `AGENTS.md` § GATE-MNT-01 line 2 + `docs/FOLLOWUP_TICKETS.md` TICKET-067 row update + this TICKET-075 Recently-closed row]; semantic swap: bidirectional `git merge-base --is-ancestor` check accepts FF-pull + post-commit-push + strict equality, rejects only true divergence where neither side is an ancestor of the other; `tools/wrap_push.sh` torna drop-in per `git push` post atomic-commit senza declared-deviation raw-push; AGENTS.md co-update rule rispettata — comment header + diagnostic-message di `tools/check_main_clean.sh` aggiornati nello stesso commit). |
| TICKET-068 | P1#5 — missing targeted crossfade+stroke regression test (Bug #5 / Fase 1#5) | 🟢 Done | `tests/text/test_crossfade_stroke.cpp` (+ `tests/core_tests.cmake` registration) | text regression. The TARGETED regression test for Bug #5 (Fase 1#5 P0) has landed. Two test cases in `tests/text/test_crossfade_stroke.cpp`: (1) crossfade with OUTGOING text LONGER than ACTIVE text establishes the OOB-precondition data shape (crossfade_layout->placed.glyphs.size() > shape->layout->placed.glyphs.size()) without crashing apply_state -- the surface that the buggy `layout.placed[gi]` would OOB on; (2) post-gap clears crossfade slots per PR 11 spec, no stale data leak forward. Registered in `tests/core_tests.cmake` `CORE_BLEND2D_TESTS` list (gated by `CHRONON3D_ENABLE_TEXT AND CHRONON3D_USE_BLEND2D`). Source fix `draw_run_layer()` stroke branch uses `source_placed.glyphs[gi].glyph_id` (active layout, bounded -- safe) instead of `layout.placed.glyphs[gi].glyph_id` (outgoing layout, OOB on longer outgoing). Discovered that the source fix is ALREADY on `main` (verified via byte-level grep on lines 492-496 of `src/backends/software/processors/text_run/text_run_processor.cpp`) -- this commit closes TICKET-068 by adding the missing Cat-2 regression test. AGENTS.md co-update rule respected (TICKET-068 row status emoji + commit reference updated in same commit). **Cat-2 E2E follow-on** added in the SAME atomic commit at `tests/text/test_draw_text_run_crossfade_stroke.cpp` (2 TEST_CASEs):(1) `draw_text_run: crossfade + stroke with longer outgoing text does not OOB (Bug #5 E2E)` — invokes `SoftwareBackend::draw_text_run` end-to-end with the OOB-precondition data shape (3-glyph active vs 33-glyph outgoing + stroke_width 2.0f magenta stroke on EVERY active glyph + paint.stroke_enabled fallback); the "does not crash" invariant is the primary regression check because Bug #5's failure mode was OOB-read on `layout.placed[gi]` (a clean dispatch proves the `glyph_id` read path stays inside the ACTIVE bounds irrespective of OUTGOING length); the dispatch may return either success OR a documented `RenderBackendErrorCode` (allow-list `InvalidInput` / `UnsupportedCapability` / `ExecutionFailure` via `is_known_render_op_error(...)`), so any return is a regression lock against the crash symptom. (2) `draw_text_run: crossfade + stroke with 33:3 outgoing ratio does not OOB (Bug #5 stress)` — deliberately pushes the asymmetry to 33:3 so any OOB-access regression fires on the FIRST draw call rather than passing through a small ratio unnoticed. Both tests skip via MESSAGE() if `tests/fixtures/Inter-Bold.ttf` is unavailable (project-wide skip convention); use `test::make_renderer()` from `tests/helpers/test_utils.hpp` to wire the SoftwareRenderer+backend bundle (necessary because draw_text_run loud-fails on any null REQUIRED service per TICKET-046). Registered in `tests/core_tests.cmake` `CORE_BLEND2D_TESTS` list directly after the Cat-1 entry, gated by `CHRONON3D_ENABLE_TEXT AND CHRONON3D_USE_BLEND2D`. Mirrors the data-shape + dispatch-path layered coverage pattern used in `test_text_unit_map` + `test_glyph_atlas_metadata`.

| TICKET-100 | text cat-3 #1 — eliminate legacy `materialize_text_run_shape()` pipeline; replace inline cache lookup + `shape_text()` + `resolve_placed_glyph_run()` + manual `TextRunLayout` build + manual cache store with a single `compile_text_layout()` call. Removes redundant `static TextLayoutCache cache;` if still inline. Updates the header comment that already declares the refactor completed to match reality. Closes review P0 #1 ("il materializzatore continua ad avere la propria pipeline indipendente"). | 🟢 Done | `src/backends/text/text_run_shape_initializer.*` (or analogous where `materialize_text_run_shape` lives) + `src/text/compile_text_layout.*` + new `tests/text/test_compile_text_layout_identity.cpp` | cat-3 freeze-fit (Rimozione percorsi legacy — dead code; zero public API expansion in `include/chronon3d/`; narrows the bone materializer to a thin wrapper). |
| TICKET-101 | text cat-3 #2 — `compile_text_layout()` accepts `const TextDocument& document + std::size_t paragraph_index` (or `const ResolvedParagraph&`) instead of reconstructing a synthetic `TextDocument para_doc` per paragraph inside the `build_text_run()` multi-paragraph wrapper. Span / font override / font-size-per-span / tracking-per-span / paragraph style / direction / language / feature-set all preserved (no silent loss). Closes review P0 #2 (rich-text info destroyed by wrapper). | Done | `src/text/text_run_builder.*` + `src/text/compile_text_layout.*` + new `tests/text/test_rich_text_paragraph_preservation.cpp` | cat-3 freeze-fit (internal refactor only; `UnsupportedMultiFontRun` exception must now originate from the public wrapper, not be circumventable). |
| TICKET-102 | text cat-3 #3 — per-paragraph error results without compression. Introduce internal `struct CompiledParagraphResult { std::size_t source_index; Result<SharedTextRunLayout, TextLayoutError> result; }` and `struct TextDocumentCompileResult { std::vector<CompiledParagraphResult> paragraphs; bool complete; }` in `src/text/` (NEVER in `include/chronon3d/`). Removes the `spdlog::warn(...) + continue` from `build_text_run()`. Fixes the misaligned-index pairing in `apply_spacing_collapse()` (`tree.paragraphs[i]` no longer assumed to match `result.paragraphs[i]` after a paragraph skip). Closes review P0 #3. | 🔵 Planned | `src/text/compile_text_layout.*` + `src/text/text_run_builder.*` + new `tests/text/test_compile_text_layout_per_paragraph_failure.cpp` | cat-3 freeze-fit (internal types only — zero public surface expansion). |
| TICKET-103a | text cat-3 #4 (freeze-minimum subset of review P0 #4) — extend the existing `TextLayoutRequest` with `TextDirection direction`, `Bcp47LanguageTag language`, `TextShapingFeatures features` (extends existing type — ZERO new public symbols). Remove the `key.direction = TextDirection::Auto` and `key.language.clear()` overrides in the canonical builder. Cache key must honor all 3 fields. Required tests: same text + font but `direction=LTR` vs `RTL` → cache miss; same text + font but `language=ar` vs `en` → cache miss. Full unification on a single `ResolvedFontAsset` (top half of review P0 #4) **deferred** as cat-violator post-baseline-verde — out of scope for the freeze-aligned minimum. | 🟢 Done | `include/chronon3d/text/text_run.hpp` (Bcp47LanguageTag + TextShapingFeatures aliases + new `features` field on TextRunLayout + TextLayoutCacheKey) + `include/chronon3d/text/text_run_builder.hpp` (3 POD fields on TextLayoutRequest) + `src/text/text_run_builder.cpp` (build_cache_key 6-param + remove force-overrides + 2 call-site propagations) + `src/text/text_run.cpp` (digest/layout_hash/shaping_hash honor features) + new `tests/text/test_layout_cache_collision.cpp` | cat-3 freeze-fit (extends existing types only — 2 std::string aliases as named semantic tags + 3 POD fields; existing aggregate-init call-sites backward-compatible via default member init). |
| TICKET-104 | text cat-3 #5 — `PendingTextRun::consumed` enforced as a real decrement of the pending state. `commit()` validates that a `.selector(...)` chained without any `.animator(...)` drops the orphan selectors + emits `spdlog::warn`. `mutable_pending()` stays public (back-compat 12+ existing sites in `tests/authoring/test_animator_dsl.cpp` + the authoring façade) but the HPP doc-comment now flags it as a bypass of the canonical consumed-decrement path. Closes review P1 #7 (false-success cluster in `PendingTextRun`). | 🟢 Done | new `src/text/pending_text_run_impl.{hpp,cpp}` (text_internal::mark_consumed + atomic counter diagnostic) + `read_spec.header` doc-comment update on `PendingTextRun::consumed` + `mutable_pending()` + `src/text/CMakeLists.txt` (TU registration) + `src/scene/builders/layer_builder.cpp` (call site: mark_consumed replaces `spec.consumed = true` silent no-op) + `src/scene/builders/text_run_builder.cpp` (#include + selector/animator chain validation in commit() + mark_consumed at end) + new `tests/text/test_builder_consumed_commit_validation.cpp` | cat-3 freeze-fit (internal hygiene — zero public API surface change; helper declaration stays out of public header). |
| TICKET-105 | text cat-2 -- identity / preservation regression test suite expansion (3 deterministic TEST_CASEs in `tests/text/test_text_unit_map_joint_include.cpp`). (1) `materialize_text_run_shape` ≡ `compile_text_layout` cache identity across frames (frame N ≡ frame N+1 scramble / morph / crossfade): same `shared_ptr<TextRunLayout>` (cache hit) AND same `layout_hash()` (cache key stable); (2) joint-include contract (canonical-only mode): canonical `text_unit_map.hpp` compiles cleanly in this TU with the 8-level ladder observable (byte/codepoint/grapheme/glyph/word/line/paragraph/span counts); the ODR conflict with the narrow `struct chronon3d::TextUnitMap` in `glyph_selector.hpp` is documented at the header itself and full resolution is deferred to TICKET-083 (post-baseline-verde, cat-violator today).  Narrow compile contract is exercised by sibling test files at single-header TU boundaries (`tests/text/test_glyph_selector_spec.cpp` + `tests/text/test_text_unit_map.cpp` + `tests/text/test_text_unit_map_8level.cpp`).  (3) double-include safety: triple `#include <chronon3d/text/text_unit_map.hpp>` at file scope; compile-time success IS the regression lock for `#pragma once` working.  Strict deterministic (no threads, no time, no PRNG — cat-2 freeze-fit).  Registered in `tests/core_tests.cmake` `CORE_BLEND2D_TESTS` list, gated by `CHRONON3D_ENABLE_TEXT AND CHRONON3D_USE_BLEND2D`.  Split-mode rationale: the canonical-class vs narrow-struct ODR conflict cannot be resolved via a single TU's include-set without touching the public selector API (cat-violator of AGENTS.md v0.1 freeze).  Full joint-include resolution ships with TICKET-083 post-baseline-verde. | 🟢 Done | 1 atomic commit. |

> **Ordinamento tabella top-10**: priorita `gate-impact desc` (regole AGENTS.md #4).
> Priority order = blocco diretto dei gate `Baseline verde: CERTIFICATA` (gate 1-9 RELEASE_GATE).
> Per espandere la copertura o riordinare, applicare stessa euristica sui ticket deferred listati sotto.

## Sprint-suspended (meta-tickets)

> Le sospensioni di sprint documentate qui sotto non sono "ticket di lavoro" nel senso top-10 ma
> **meta-tickets di stato**: tracciano l'halt di un piano di migrazione in attesa di una
> condizione esterna (certificazione baseline verde, revoca formale del freeze, ecc.).
> Convenzione: lo schema-status resta canonico (`🔵 OPEN`) e lo `SUSPENDED` è un
> qualificatore testuale a chiarezza.

| ID | Area | Stato | File / Contesto |
|---|---|---|---|
| TICKET-066 | 12-ATOMO Single Selector Pipeline migration plan | 🔵 OPEN — **SUSPENDED** pending baseline verde | `docs/TEXT_SELECTOR_SINGLE_PIPELINE_PLAN.md` + downstream `include/chronon3d/text/{glyph_selector,glyph_selector_spec,compiled_selector,glyph_selector_evaluator,animation/text_animator_spec}*`. ATOMO 5/6/7/8 rolled-back per freeze `AGENTS.md` v0.1 (VIETATO: nuovi `#include`, nuove classi pubbliche, modifiche che espandono la superficie API); ATOMO 2 work preservato intatto in `stash@{0}`. |

## Other open (deferred — see archive)

29 ticket aperti non-baseline-critical. Per `**Status**` reale + root cause + risoluzione,
vedi [`docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md`](docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md)
o `git show 6f3309e6:docs/FOLLOWUP_TICKETS.md` (pre-Step-6 source-of-truth, 3834-line).

| ID | Area |
|---|---|
| TICKET-EXP2-G3 | ExpressiveV2 |
| TICKET-009 | experimental subtree |
| TICKET-010 | experimental subtree |
| TICKET-012 | Arch violation: forbidden include |
| TICKET-013 | Arch violation: sanction bypass |
| TICKET-014 | Test fail: render_session_reset |
| TICKET-015 | Test fail |
| TICKET-016 | Test fail |
| TICKET-017 | Re-enable TICKET-007.c cycle detection |
| TICKET-018 | Re-enable temporal frame-keyed jitter |
| TICKET-019 | Re-enable motion-blur torture |
| TICKET-021 | PoseTracksSource Zoom over FOV/Physical |
| TICKET-023 | OrientAlongPath stub |
| TICKET-025 | Trajectory hardcoded zoom/fov |
| TICKET-027 | ShotTimeline diagnostics |
| TICKET-028 | Constraint stateful without Checkpoint |
| TICKET-033 | CameraDescriptor payload on LayerKind::Camera |
| TICKET-034 | CameraDescriptor as canonical default |
| TICKET-035 | Framebuffer lacks bytes |
| TICKET-041 | Sync cmake/Chronon3DRegistry |
| TICKET-042 | Sync vcpkg.json deps with CMakeLists.txt |
| TICKET-043 | Audit apps/* includes |
| TICKET-045 | tools/check_gitignored_dirs + audit_software |
| TICKET-047 | tools/test_architectural.sh Section X |
| TICKET-054 | build-fast.sh target forwarding |
| TICKET-055 | Per-composition keyframe PNG forensic glyph |
| TICKET-056 | A4.4 + A4.5 memory envelope tuning |
| TICKET-057 | Drop tests/helpers/render_fixtures.hpp |
| TICKET-078 | FASE 3 text_rasterizer_render.cpp routing via TextRenderResources (singleton `Blend2DResources` removal) | 🔵 Planned (deferred post-baseline-verde) | src/backends/text/text_rasterizer_render.cpp + include/chronon3d/backends/text/text_rasterizer_utils.hpp | La rimozione del singleton `Blend2DResources` interno a `rasterize_text_to_bl_image` richiede AGGIUNTA DI PARAMETRO `TextRenderResources&` alla firma della funzione (oggi a 8 parametri, dichiarati in `include/chronon3d/backends/text/text_rasterizer_utils.hpp:52`), VIOLANDO AGENTS.md v0.1 freeze (`Qualsiasi modifica a include/chronon3d/ che espanda la superficie API`). Workaround corrente: il singleton `blend2d_resources()` continua a fornire il cache locale anonimo (linee 152-153 di text_rasterizer_render.cpp); `BLFontFaceCache` già esiste come membro canonico di `TextRenderResources` ed è cablato a `SoftwareProcessorContext::text_resources` ma non ancora consumato da `rasterize_text_to_bl_image`. Riammesso dopo `Baseline verde: CERTIFICATA 11/11` + revoca formale del freeze da un commit esplicito. Sub-task C (std::call_once elimination) chiuso a parte — vedi TICKET-077. Sub-task A/B (TextServices umbrella + RenderRuntime capture di campo) deferred a parte — vedi TICKET-079. |
| TICKET-079 | FASE 3 umbrella `TextServices` + `FontStore` + `TextRasterCache` + `RenderRuntime::owned<TextServices*>` | 🔵 Planned (deferred post-baseline-verde) | NEW include/chronon3d/runtime/text_services.hpp + edit include/chronon3d/runtime/render_runtime.hpp | Nuove classi pubbliche `class TextServices / class FontStore / class TextRasterCache` in `include/chronon3d/runtime/` VIOLANO AGENTS.md v0.1 freeze (`VIETATO: nuovi #include, nuove classi pubbliche, ... qualsiasi modifica che espanda la superficie API`). Stesso divieto per `RenderRuntime::owned<TextServices*>` (nuovo campo pubblico di RenderRuntime = espansione superficie). L'infrastruttura di base ESISTE GIÀ: `BLFontFaceCache`/`FreeTypeFaceCache`/`GlyphOutlineBuilder`/`TextScratchManager` come membri di `TextRenderResources`; `struct TextLayoutCache`; `GlyphAtlasCache<LruCache<GlyphAtlasKey,GlyphAtlasEntry,GlyphAtlasKeyHash>>`. Il wrapper umbrella `TextServices` non aggiunge capacità, solo ri-aggregazione. Riammesso dopo `Baseline verde: CERTIFICATA 11/11` + revoca formale del freeze da un commit esplicito. Sub-task C (std::call_once) chiuso a parte — vedi TICKET-077. Sub-task D (rasterize_text_to_bl_image routing) deferred a parte — vesi TICKET-078. |
| TICKET-081a | FASE 2 grapheme_break extraction (Cat-3 deferred post-baseline-verde) | 🔵 Planned | src/text/unicode/grapheme_break.{hpp,cpp} + edit src/text/text_unit_map.cpp + edit include/chronon3d/backends/text/text_unicode_utils.hpp | Consolidation desumes the GB-classifier properties (is_combining_mark / is_zwj / is_regional_indicator / is_extended_pictographic) that currently live inline in `src/text/text_unit_map.cpp` anonymous namespace AND the parallel `is_grapheme_extend`, `is_extended_pictographic`, `is_regional_indicator`, `grapheme_cluster_count`, `grapheme_byte_offset_at` definitions in the PUBLIC `include/chronon3d/backends/text/text_unicode_utils.hpp`. The public-header side requires either (a) inline forwarder delegation (zero surface change, recommended) OR (b) icon removal from public header (NOT cat-3, would be public-API shrink — viable but must be coordinated with consumer SDK install gate). Until baseline verde, no_option_(b) blocks the merge. Cat-3 deferred for split-target sequencing: TICKET-080 (atomic consolidation of utf8_decoder + whitespace) committed on `main` first; TICKET-081a + TICKET-081b + TICKET-082 land in follow-up after baseline verte. Sub-task A (selector + animator migration to canonical 8-level TextUnitMap) deferred to TICKET-083 — see below. |
| TICKET-081b | FASE 2 word_break extraction (Cat-3 deferred post-baseline-verde) | 🔵 Planned | NEW src/text/unicode/word_break.{hpp,cpp} + edit src/text/text_unit_map.cpp | Extraction lifts the WB5a state machine currently inlined in `src/text/text_unit_map.cpp` (wo separate passes over `placed.clusters`: WB5a break detection + whitespace-run post-fixup) to a standalone `word_break` module with `(utf8_sv, clusters) → word_index_per_glyph` + `word_boundary_at(utf8_sv, byte_offset)→ enum class`. New module adds a public-API surface in include/chronon3d/text/ IF exported (Cat-3 alternative: keep it internal to `src/text/`, as per TICKET-080 pattern — preferred). WB rule coverage (WB1, WB5a whitespace, WB999) is bounded subset per docs/ANTI_DUPLICATION_RULES.md (no ICU/Boost import). Deferred post-baseline-verde per AGENTS.md v0.1 freeze. |
| TICKET-082 | FASE 2 Cat-2 regression harness for `src/text/unicode/` (deferred post-TICKET-011 build rot fix) | 🔵 Planned | NEW tests/text/test_text_unicode_canonical.cpp + register in tests/core_tests.cmake | Cat-2 deterministic regression tests for the canonical `utf8_decoder` + `whitespace` consolidation (TICKET-080). TEST_CASEs to lock: ASCII fastpath (`'A' → 0x41`), 4-byte UTF-8 (`'😀' → 0x1F600`), 0x2028 LINE SEPARATOR `→ true`, 0x2029 PARAGRAPH SEPARATOR `→ true`, 0xFEFF BOM `→ true`, `decode_codepoint` vs `decode_codepoint_at` equivalence. Pre-build rot (TICKET-011) prevented landing this in TICKET-080 commit; once TICKET-011 is closed, this Cat-2 follow-up ships in the SAME atomic commit that closes TICKET-080's harness gap. Defer until baseline verde. AGENTS.md v0.1 Cat-2 freeze-compliant — strict deterministic, no threads, no time, no PRNG. |
| TICKET-083 | FASE 2 selector + animator migration to canonical 8-level TextUnitMap (deferred Cat-violator) | 🔵 Planned | include/chronon3d/text/{glyph_selector, animation/text_animator_stack, animation/text_animator_evaluator}.{hpp,cpp} | Migration desumes the narrow 3-level `struct TextUnitMap` (`glyph_to_grapheme/word/line + first_glyph_for_word/line + grapheme_count/word_count/line_count`, in `include/chronon3d/text/glyph_selector.hpp:94`) to the canonical 8-level `class TextUnitMap` (`include/chronon3d/text/text_unit_map.hpp:120`, byte→codepoint→grapheme→glyph→word→line→paragraph→span + identity_at_byte ladder). Requires: (1) forward-decl switch in `text_animator_stack.hpp:13` from `chronon3d/text/glyph_selector.hpp` to `chronon3d/text/text_unit_map.hpp`; (2) `compile_text_layout` in `src/text/text_run_builder.cpp` to populate the canonical `units` field (currently emits the narrow one via `build_text_unit_map`); (3) per-callsite parse of `unit_index(glyph_index, unit)` → migrates to canonical `byte_to_codepoint/glyph_to_word/etc.`. Each of (1)(2)(3) IS a public-API change in `include/chronon3d/text/` → VIOLATES AGENTS.md v0.1. Deferred post-baseline-verde + formal freeze revocation per AGENTS.md §Feature Freeze — Revoca. |
| TICKET-084 | FASE 2 `text::unicode::Result<TextUnitMap, TextUnitMapError>` migration (deferred Cat-violator) | 🔵 Planned | include/chronon3d/text/text_unit_map.hpp + src/text/text_unit_map.cpp + include/chronon3d/runtime/execution_core_result.hpp | Adds `enum class TextUnitMapError { … }` to `include/chronon3d/text/text_unit_map.hpp` and changes the constructor's signature from `TextUnitMap(utf8, placed, …)` to `Result<TextUnitMap, TextUnitMapError> build(utf8, placed, …)`. NEW `enum class` in public header = VIOLATES AGENTS.md v0.1 freeze ("nessuna modifica a `include/chronon3d/` che espanda la superficie API"). The superordinate FASE 2 sub-task is "unify error model across TextUnitMap + TextCompileServices + TextAnimatorSpec via `Result<T,E>` end-to-end" — multi-class syntonisation that requires public API expansion on 3+ headers. Deferred post-baseline-verde. |
| TICKET-085 | FASE 2 shaped-glyph count fix + incremental RI algorithm (deferred NEW FEATURE blocks) | 🔵 Planned | NEW src/text/text_ri.{hpp,cpp} + edits to include/chronon3d/text/glyph_selector.hpp + include/chronon3d/text/animation/text_animator_spec.hpp + tests/text/test_text_ri.cpp | (a) Shaped-glyph count fix: address the discrepancy between `TextUnitMap::glyph_count_for_word(word_idx)` and the rendered glyph count delivered by the shaper when the same `word_idx` contains bracketing combining marks bound to different grapheme clusters. Requires new counter passing through `PlacedGlyphRun::Cluster::glyph_count`. (b) Incremental RI: relevance-increment scoring algorithm for typewriter/text-audit incremental search; no precedent in the tracked tree (no `ri_score` / `ri_increment` / `fuzzy_match` / `relevance_increment` exists today, per FASE 2 audit). Both sub-tasks are NEW FEATURE implementation per AGENTS.md v0.1 ("nuove feature … animazioni, … nuovi preset, registry, risolutori, sampler"). VIOLATES freeze. Deferred post-baseline-verde + formal freeze revocation. |

| TICKET-091 | FASE 4 CompiledTextAnimatorProgram (one-time per-layout compilation) | 🔵 Planned (deferred post-baseline-verde) | NEW include/chronon3d/text/animation/compiled_text_animator_program.hpp + edits to include/chronon3d/text/animation/text_animator_stack.hpp + tests/text/test_compiled_animator_program.cpp | Replaces per-frame stack rebuild with a `class CompiledTextAnimatorProgram` holding precompiled binding closures (`SafeAccessMap` snapshot, per-glyph unit-handle lookup tables, sub-frame sample-tick cache) keyed by `TextRunLayout`. NEW public class in `include/chronon3d/` VIOLATES AGENTS.md v0.1 freeze ("nuove classi pubbliche"). Deferred post-baseline-verde + formal freeze revocation. |
| TICKET-093 | FASE 4 tier vector + shadow images — surface pool completion in TextScratchManager | 🔵 Planned (deferred post-baseline-verde) | src/backends/text/text_render_resources.hpp + src/backends/text/text_render_resources.cpp + src/backends/software/processors/text_run/text_run_processor.cpp | FASE 3 already added `TextScratchManager` + `SurfaceEntry` pool + per-call scratch RAII handles. FASE 4 closes the remaining coverage gaps: explicit `tier_buffer` slot allocation in `TextScratchState` (currently `scratch_state.acquire_surface()` is implemented but the tier-only slot accounting lives on the ScratchManager; decoupling surface-alloc from tier-image bookkeeping). Public API exposure (`scratch_state.tier_buffer_count()` accessor) IS a public-header surface addition → VIOLATES freeze v0.1. Deferred post-baseline-verde. (TICKET-090 + TICKET-090b already extracted the orchestrator stages that consume the tier vector — TICKET-093 is the pool-side completion.) |
| TICKET-094 | FASE 4 FontFaceLease per-face (shaping fuori dal lock globale) | 🔵 Planned (deferred post-baseline-verde) | include/chronon3d/backends/text/font_engine.hpp + src/backends/text/font_engine.cpp + include/chronon3d/backends/text/font_face_lease.hpp | Introduces per-`FaceEntry` ref-count lease so concurrent `shape_text()` calls for different font faces run without serializing on the global `face_cache_mutex`. Requires (a) `class FontFaceLease { FaceEntry*; uint32_t ref_count; }` RAII type with lease acquire / release, (b) narrowing the `unique_lock` in `shape_text()` to JUST the `face_cache.find/emplace` block (entry ptr is stable for the function's lifetime when actively leased), and (c) destruction-path coordination so a concurrent `clear_cache()` cannot race a live lease. NEW public class `FontFaceLease` in `include/chronon3d/backends/text/font_face_lease.hpp` VIOLATES AGENTS.md v0.1 freeze ("nuove classi pubbliche"). Subsumes TICKET-088b (lock narrowing coupled migration). Also unblocks TICKET-088's HB pool benefit on multi-thread concurrent shapes. Deferred post-baseline-verde + formal freeze revocation. |
| TICKET-095 | FASE 4 measure_text_fast() (cheap measure path bypassing shape_text) | 🔵 Planned (deferred post-baseline-verde) | src/backends/text/font_engine.cpp (implementation) + include/chronon3d/text/wrap_layout.hpp (exposure) | Provides the layout-time `measure_text` path with a fast O(N) advance-only HarfBuzz call (no per-glyph position resolution, no FT bbox lookup, no GlyphRun assembly) for cases that ONLY need total advance width (e.g., text-fit, line-break decisions). Currently `measure_text()` calls `shape_text()` returning full `GlyphRun`. The fast path uses only `hb_buffer_add_utf8` + `hb_shape` → `hb_glyph_position_get_x_advance()` accumulation. Public API add (`measure_text_fast()` + optional `class TextWrapLayout`) VIOLATES AGENTS.md v0.1 freeze. Deferred post-baseline-verde. |
| TICKET-096 | FASE 4 remove external `std::shared_mutex` from glyph atlas + text rasterizer cache | 🔵 Planned (deferred post-baseline-verde) | src/backends/text/glyph_atlas.cpp + src/backends/text/text_rasterizer_cache.cpp + include/chronon3d/backends/text/text_render_resources.hpp | Removes the standalone `std::shared_mutex` member currently exposed on `GlyphAtlasCache` + `GlyphOutlineCache` + `TextRasterCache` (each subclass owns a private mutex). Consolidation goal: route all read/write access through the engine-owned `TextRenderResources::lock()` RAII guards (one path per cache lookup) so external `shared_lock(unique_lock)` callers are eliminated. Implementation requires new public RAII accessor `TextRenderResources::lock_text_cache()` on the cache class itself → VIOLATES AGENTS.md v0.1 freeze ("nessuna modifica a `include/chronon3d/` che espanda la superficie API"). Deferred post-baseline-verde. |
| TICKET-097 | FASE 4 Cat-2 regression harness for draw_text_run stages + HB pool | 🔵 Planned (deferred post-TICKET-011 build rot) | NEW tests/text/test_text_stage_harness.cpp + register in tests/core_tests.cmake | Cat-2 deterministic tests for TICKET-088 (HB pool re-entrance: 2 acquire→release cycles on different hb_buffer_t* pointers + sentinel for `hb_buffer_reset()` clearing the prior payload) + TICKET-090 (`expand_per_glyph_bbox` on synthetic vectors + `apply_separable_box_blur` on seeded PRGB32 patterns). Required by AGENTS.md v0.1 Cat-2 freeze ("Test deterministici — golden test, sentinel hash, regression gate"). Pre-build rot (TICKET-011) prevents landing in TICKET-088/090 atomic commits; defer until baseline verde + TICKET-011 closure. |
| TICKET-098 | FASE 5 text_preset_registry per-category register helpers (Cat-3) | 🟢 Done | src/registry/text_preset_registry.cpp | 4 per-category register helpers (`register_text_preset_cinematic` x4 / `_reveal` x10 / `_emphasis` x4 / `_subtitle` x4) added inside the existing anonymous namespace of the registry's implementation TU. `register_builtin_presets(r)` now delegates to the 4 helpers (insertion order: Cinematic → Reveal → Emphasis → Subtitle, preserving the original sequence). The 22 `entry()` factories + 22 `compose_<id>()` helpers stay colocated inside the anon namespace (no extraction, no fabrication, no public API change). Internal-linkage (anon namespace): not visible to other TUs. AGENTS.md v0.1 Cat-3 freeze-aligned ("anti-duplication semantic split per categoria"). The full file-level extraction (which would move 1100+ lines into 4 separate .cpp files, each with its own share of `text_preset_registry_internal.hpp`) is deferred as TICKET-104 because it requires new internal CMakeLists.txt wiring + per-TU forward declarations. Adoption tests can route via `register_builtin_presets(r)` followed by `by_category(TextPresetCategory::X)` queries (the per-category registers are reachable internally but stay file-private). |

| TICKET-099a | FASE 5 internal-only domain type aliases (Cat-3) | 🟢 Done | NEW src/text/aliases.hpp | 7 typed aliases over existing C++ primitives in `namespace chronon3d::text::domain`: `FontAssetId` = `std::string_view`, `ResolvedFontPath` = `std::string`, `FontFamilyName` = `std::string`, `Bcp47LanguageTag` = `std::string`, `TextPresetId` = `std::string`, `SelectorId` = `std::string`, `FontWeight` = `int`. Lives strictly under `src/text/` (NOT in `include/chronon3d/`) — internal-only. Same bytewise layout as underlying primitives; typedefs do not change runtime representation. AGENTS.md Gate 5 deny-list: no `<unicode/...>` nor `<libtess2>` includes (only `<string>` + `<string_view>`). Adoption deferred: TYPIFIED call sites that WILL live in post-baseline TICKET-101..106 implementations (resolver, selector, registry by_category-keying). Currently no consumer call sites (intentional scaffolding) — adopter commits in TICKET-101/102/103 will route through these aliases. |

| TICKET-101 | text cat-3 #2 -- `compile_text_layout()` accepts `const TextDocument& document + std::size_t paragraph_index` (via new `paragraph_index` field on `TextLayoutRequest`, zero new public classes) instead of reconstructing a synthetic `TextDocument para_doc` per paragraph inside the `build_text_run()` multi-paragraph wrapper. Span / font override / font-size-per-span / tracking-per-span / paragraph style / direction / language all preserved (no silent loss). `build_text_run()` wrapper rejects multi-font paragraphs at the public API surface (verdict Issue #3 closure). Closes review P0 #2. | Done (atomic on `main` per AGENTS.md v0.1 freeze-compliant cat-3 refactor) | `include/chronon3d/text/text_run_builder.hpp` + `src/text/text_run_builder.cpp` + new `tests/text/test_rich_text_paragraph_preservation.cpp` | cat-3 freeze-fit (POD extension `paragraph_index` to existing struct; zero new public classes; eliminates rich-text info destruction in wrapper; direction/language/line_height propagated from `comp_style`). |
| TICKET-092 | text cat-3 #3 -- per-paragraph error accumulator (CompiledParagraphResult + TextDocumentCompileResult). Internal `struct CompiledParagraphResult { std::size_t source_index; Result<std::shared_ptr<TextRunLayout>, TextLayoutError> result; }` + `struct TextDocumentCompileResult { std::vector<CompiledParagraphResult> paragraphs; bool complete; }` defined in NEW `src/text/text_document_compile_result.hpp` (NEVER in `include/chronon3d/`, per cat-3 freeze). Internal function `compile_text_document()` walks the resolved tree once, runs the multi-font pre-check, accumulates Ok AND Err per-paragraph results. Removes the `spdlog::warn(...) + goto next_paragraph` skip-on-failure pattern from `build_text_run()`. `apply_spacing_collapse()` rewritten to use `source_index` as the bridge into `tree.paragraphs[source_index].style` (no more `result.paragraphs[i] == tree.paragraphs[i]` assumption that broke after a paragraph skip). Closes review P0 #3. | 🟢 Done | new `src/text/text_document_compile_result.hpp` + `src/text/text_run_builder.cpp` (compile_text_document + apply_spacing_collapse rewrite + build_text_run thin wrapper) + new `tests/text/test_compile_text_layout_per_paragraph_failure.cpp` | cat-3 freeze-fit (internal types only -- zero public surface expansion; per-paragraph failure preserved with source_index bridge; spacing_collapse chain breaks cleanly at Err paragraphs; Ok-only public TextRunBuildResult preserved for backward compat with 10+ existing call sites; build_text_run still emits the per-paragraph diagnostic that log-scrapers expect). |
| TICKET-103a | text cat-3 #4 -- extend `TextLayoutRequest` with `direction` + `language` + `features` (POD extensions, zero new classes). Remove the `key.direction = Auto` and `key.language.clear()` overrides in `build_cache_key()`; cache key signature now honors all 3 fields. 2 deterministic TEST_CASEs in `tests/text/test_layout_cache_collision.cpp`: (1) same text+font `direction=LTR` vs `RTL` -> `digest()` differ (bidi cache-collision contract); (2) same text+font+direction `language=en` vs `ar` -> `digest()` differ (BCP-47 cache-collision contract). Pure key-construction tests, no font engine required. TICKET-093 (ResolvedFontAsset unification) deferred post-baseline-verde. | 🟢 Done | 1 atomic commit. |
| TICKET-104 | text cat-3 #5 -- `PendingTextRun::consumed` enforced as a real process-wide decrement (was documented no-op in the header comment); selector/animator chain validation in `TextRunBuilder::commit()` (selector without animator drops orphan + spdlog::warn). `mutable_pending()` stays public for back-compat (12+ existing sites) but HPP doc-comment now flags it as a bypass of the canonical consumed-decrement path. 2 deterministic TEST_CASEs in `tests/text/test_builder_consumed_commit_validation.cpp`: (1) `.selector(...).commit()` without `.animator(...)` -> `spec.params.animators.size() == 0u` (locks orphan-drop contract, spdlog::warn emitted); (2) `mark_consumed(run)` flips flag AND `consumed_decrement_count()` increments by 1 (locks real-decrement contract; pre-TICKET-104 flag flip was silent counter-frozen). Pure internal-state deterministic tests, no font engine / scene build required. Closes review P1 #7 (false-success cluster in `PendingTextRun`). | 🟢 Done | 1 atomic commit. |
| TICKET-105 | text cat-2 -- identity / preservation regression test suite expansion (3 deterministic TEST_CASEs in `tests/text/test_text_unit_map_joint_include.cpp`). (1) `materialize_text_run_shape` ≡ `compile_text_layout` cache identity across frames (frame N ≡ frame N+1 scramble / morph / crossfade): same `shared_ptr<TextRunLayout>` (cache hit) AND same `layout_hash()` (cache key stable); (2) joint-include contract (canonical-only mode): canonical `text_unit_map.hpp` compiles cleanly in this TU with the 8-level ladder observable (byte/codepoint/grapheme/glyph/word/line/paragraph/span counts); the ODR conflict with the narrow `struct TextUnitMap` in `glyph_selector.hpp` is documented at the header itself and full resolution is deferred to TICKET-083 (post-baseline-verde, cat-violator today).  Narrow compile contract is exercised by sibling test files at single-header TU boundaries (`tests/text/test_glyph_selector_spec.cpp` + `tests/text/test_text_unit_map.cpp` + `tests/text/test_text_unit_map_8level.cpp`).  (3) double-include safety: triple `#include <chronon3d/text/text_unit_map.hpp>` at file scope; compile-time success IS the regression lock for `#pragma once` working.  Strict deterministic (no threads, no time, no PRNG — cat-2 freeze-fit).  Registered in `tests/core_tests.cmake` `CORE_BLEND2D_TESTS` list, gated by `CHRONON3D_ENABLE_TEXT AND CHRONON3D_USE_BLEND2D`.  Split-mode rationale: the canonical-class vs narrow-struct ODR conflict cannot be resolved via a single TU's include-set without touching the public selector API (cat-violator of AGENTS.md v0.1 freeze).  Full joint-include resolution ships with TICKET-083 post-baseline-verde. | 🟢 Done | 1 atomic commit. |

| TICKET-107 | FASE 5 per-category register helpers promoted out of anon-namespace (Cat-3) | 🟢 Done | src/registry/text_preset_register_helpers.hpp + src/registry/text_preset_registry.cpp + tests/test_text_preset_registry.cpp | New `src/registry/text_preset_register_helpers.hpp` declares the 4 per-category helpers + `register_builtin_presets` umbrella in `namespace chronon3d::registry::register_helpers_internal` (with public re-exports at the parent `chronon3d::registry` namespace). The 4 helpers + umbrella are lifted out of the file-local anonymous namespace in `src/registry/text_preset_registry.cpp`; the 22 `entry()` factories + 22 `compose_<id>()` helpers + `wire_through_resolver` body stay in the anon block (still file-local). 4 new SUBCASEs (50-53) in `tests/test_text_preset_registry.cpp` exercise the helpers in isolation: per-category distribution, umbrella match, composability, idempotency-throws. No public API expansion (Cat-3 freeze-aligned). Reviewer-driven follow-up to TICKET-098 (which had anon-namespace helpers unreachable from other TUs). |
| TICKET-108a | FASE 5 aliases.hpp 7 typedefs adopted via internal text_resolver_helpers (Cat-3) | 🟢 Done | src/text/internal/text_resolver_helpers.{hpp,cpp} + src/text/CMakeLists.txt | New `src/text/internal/text_resolver_helpers.hpp` + .cpp implement 6 internal helper functions that USE the 7 aliases from `src/text/aliases.hpp` (FontAssetId / ResolvedFontPath / FontFamilyName / Bcp47LanguageTag / TextPresetId / SelectorId / FontWeight). The functions: `validate_bcp47_language_tag`, `canonicalize_bcp47_language_tag`, `parse_font_weight`, `make_resolved_font_path`, `make_font_family_name`, `make_text_preset_id`, `make_selector_id`. Added to `chronon3d_text_core` OBJECT library. No `<unicode/...>` or `<libtess2>` includes (Gate 5 deny-list clean). The aliases are no longer dead code — they appear in 6 function signatures + `parse_font_weight(FontWeight)`. Internal-only — not installed. Reviewer-driven follow-up to TICKET-099a (which left the typedefs with no callers). **FASE 5 follow-up (TICKET-101/108a):** wired 5 of 7 helpers into real production callers in `src/text/text_resolver.cpp` (Cat-3 freeze-compliant, no public API change): (1) `make_font_family_name` + `parse_font_weight` in `resolve_fallback_fonts` and `effective_font` via new anon-namespace helper `apply_fontspec_canonicalization`; (2) `validate_bcp47_language_tag` + `canonicalize_bcp47_language_tag` in `resolve_text_run_tree` (at source — canonical value flows to all downstream consumers of `run.paragraph_style.language` / `shaped_para.style.language`) + `shape_resolved_run` (defense-in-depth for direct callers). 2 helpers skipped in this TU: `make_resolved_font_path` (needs `assets_root` — no AssetResolver in this TU) + `make_text_preset_id` / `make_selector_id` (no registry lookups in this TU). AnimatedValue<FontSpec> evaluate-path wiring was attempted but REVERTED per code-reviewer MAJOR #1 (anti-duplication: canonicalization logic inlined verbatim violates `docs/ANTI_DUPLICATION_RULES.md`) + MAJOR #2 (Cat-3 freeze borderline: branch adds observable behavior to a public template method). Deferred to post-baseline-verde as TICKET-115 follow-up: hoist `apply_fontspec_canonicalization` into a public `chronon3d::canonicalize_fontspec(FontSpec&)` free function in `include/chronon3d/text/font_engine.hpp`, then add the AnimatedValue<FontSpec> branch as a one-liner call. 4 new regression tests in `tests/text/test_text_resolver.cpp` lock the wiring: font_family whitespace trim, invalid font_weight → 400, valid font_weight → preserved, BCP-47 lowercased at source. |
| TICKET-109 | FASE 5 layered TextRunBuilderInspector + migrate 53 mutable_pending() callsites (Cat-2 + Cat-3) | 🟢 Done | tests/support/text_run_builder_inspection.hpp + tests/authoring/test_animator_dsl.cpp | New `tests/support/text_run_builder_inspection.hpp` mirrors the established `LayerBuilderInspector` pattern. Defines a value-typed `TextRunSnapshot` (name field + non-owning `PendingTextRun*` + `operator->()` forwarding for ergonomic field chain) and a static `TextRunBuilderInspector::pending_of(t)` method. Migrated 53 `IDENT.mutable_pending()` callsites in `tests/authoring/test_animator_dsl.cpp` to `TextRunBuilderInspector::pending_of(IDENT)`. The address-compare test was rewritten to use the value-typed `name` field (a stronger distinctness check than raw pointer compare). The public `Text::mutable_pending()` accessor is preserved (no removal in this PR); TICKET-110 (deferred) will remove the public accessor + add a `friend class TextRunBuilderInspector` declaration on `Text` in lock-step. |
| TICKET-110 | FASE 5 remove public Text::mutable_pending() + add friend class TextRunBuilderInspector (3-step lock-step) | 🔵 Planned (deferred post-baseline-verde) | include/chronon3d/authoring/text.hpp | Three-step lock-step process (reviewer finding #8): (1) add `friend class chronon3d::authoring::testing::TextRunBuilderInspector;` declaration to `Text` (new public name in `include/chronon3d/`); (2) migrate the inspector's `pending_of(t)` from `&t.pending()` to `&t.mutable_pending()` so the inspector can keep working post-friend (the inspector's call surface `TextRunBuilderInspector::pending_of(t).X` stays identical throughout); (3) remove the public `Text::mutable_pending()` accessor. VIOLATES freeze (public API change in `include/chronon3d/`) — defer to AGENTS.md v0.2 or post-baseline-verde. |
| TICKET-111 | FASE 5 Cat-2 regression test for the public re-exports of register_helpers_internal | 🔵 Planned (deferred) | tests/test_text_preset_registry.cpp (extended) | A Cat-2 test that exercises the 4 per-category helpers through the public re-exported `chronon3d::registry::register_text_preset_<category>` namespace (NOT through `register_helpers_internal::`) to lock the re-export contract. Currently the TICKET-107 SUBCASEs (50-53) call the helpers through the parent namespace (the re-export resolves transitively), so the surface contract is partially covered. A dedicated Cat-2 test for the re-exports specifically closes the surface-locator gap. Low priority. |
| TICKET-112 | FASE 5 cat-3 test for text_resolver_helpers canonicalization rules | 🔵 Planned (deferred) | tests/text/test_text_resolver_helpers.cpp (new) | Unit tests for the 6 text_resolver_helpers functions: `validate_bcp47_language_tag` accepts well-formed tags / rejects empty + embedded nulls + non-ASCII; `canonicalize_bcp47_language_tag` lowercases consistently; `parse_font_weight` accepts 100..900 in steps of 100 / rejects 0 and 1000; `make_resolved_font_path` composes paths via std::filesystem; `make_font_family_name` / `make_text_preset_id` / `make_selector_id` trim ASCII whitespace + reject empty. Low priority. |
| TICKET-113 | FASE 5 LayerBuilderInspector parity test for the TextRunBuilderInspector | 🔵 Planned (deferred) | tests/authoring/test_text_run_builder_inspector.cpp (new) | A dedicated Cat-2 test TU for `TextRunBuilderInspector::pending_of(t)` that locks the snapshot contract: `name` is value-typed, `pending` is non-owning, `operator->()` forwards to `*pending`, multiple snapshots from the same `Text` handle point to the same underlying `PendingTextRun` (identity preserved across snapshots), no-throw guarantee. Mirrors the LayerBuilderInspector parity discipline. Low priority. |
| TICKET-114 | FASE 5 promote TICKET-107/108a/109 implementation patterns to docs/CONTRIBUTING.md as the canonical "test-only inspector" + "internal-only header" patterns | 🔵 Planned (deferred) | docs/CONTRIBUTING.md (extension) | Document the two close-out patterns: (1) test-only inspectors in `tests/support/` with friend-declaration-free `pending_of(t)` / `pending_runs(lb)` shapes; (2) internal-only headers in `src/<area>/internal/` with `domain` namespace aliases + helper functions. Currently these patterns are documented inline in each new file's doc-comment but the CONTRIBUTING.md top-level doesn't have a canonical "test-only inspector" + "internal-only helpers" section. Low priority. |


## Cross-link history

Per la cronologia completa (49+ ticket chiusi + 28 deferred a follow-up, tutti con `**Status**` reale
preservato verbatim dal pre-Step-6), vedi:

- `docs/ARCHIVE/FOLLOWUP_TICKETS_HISTORY.md` — verbatim cronologia ticket chiusi + deferred.
- `git show 6f3309e6:docs/FOLLOWUP_TICKETS.md` (commit pre-Step-6 pinned) per la versione 3834-line originale.
- `docs/CHANGELOG.md` — eventi sul main.

### Recently closed (atomic `main` commits, 2026-06-29)

| ID | Area | Status | Atomic commit on `main` |
|---|---|---|---|
| TICKET-052 | TEXT-UNM-01 — real TextUnitMap 8-level identity ladder | 🟢 Done (code on main) | `feat(text): TEXT-UNM-01 — real TextUnitMap with separated identity levels` (env-vars Agent3; **§12 closure doc lost during partner rebase**, see followup SECT-12-RESTORE; **code surface intact on main HEAD**, verify via `git grep kInvariant include/chronon3d/text/glyph_selector.hpp`) |
| TICKET-053 | TEXT-SEL-01 — Range / Wiggly / Expression canonical selectors | 🟢 Done | `feat(text): TEXT-SEL-01 — Range/Wiggly/Expression selectors as canonical types` (env-vars Agent3, atomic commit on `main`) |
| TICKET-058 | Step 9 ghost_sweep ROT-2 — `RenderRuntime::executor` chain on software_renderer consumers | 🟢 Done (atomic on `main`) | `e853e728 fix(backends/software): include render_runtime.hpp for software_renderer consumers (Step 9 ghost_sweep ROT-2)` — Fix-5 sibling rot-2 mirror del Fix-2 in `81cdc738`. Audit-comment block TICKET-038/TXT-00 cita rot-1 precedent. Forbidden-list: solo `include/chronon3d/backends/software/software_renderer.hpp`. |

See:
- `docs/MIGRATION_TEXT_SPEC.md` §13 for full closure documentation (3 canonical selector types stored under `std::variant<RangeSelector, WigglySelector, ExpressionSelector>`, `std::visit` dispatch via `evaluate_selector_v2` / `evaluate_selectors_v2`, `SafeAccessMap` with `textIndex` / `textTotal` / `frame` auto-binding, 13 new TEST_CASEs at `tests/text/test_glyph_selector_spec.cpp`).
- `docs/MIGRATION_TEXT_SPEC.md` §12 (closure for TEXT-UNM-01) is **missing on disk** — followup SECT-12-RESTORE in atomi backlog to restore closure doc after partner rebase truncated it.

| TICKET-059 | TEXT-PLY-01 — 14-feature ParagraphStyle extension + wiring | 🟢 Done | `feat(text): TEXT-PLY-01 — 14-feature ParagraphStyle extension + wiring` (env-vars Agent3, atomic on `main`; §14 closure doc in `docs/MIGRATION_TEXT_SPEC.md` §14; 14 TEST_CASEs in `tests/text/test_paragraph_layout_extras.cpp`) |

| TICKET-048 | GATE-MNT-01 — wire main-sync gate as pre-push wrapper | 🟢 Done | `ops(gates): GATE-MNT-01 — wire main-sync gate as pre-push wrapper` (env-vars Agent3, atomic on `main`; 4 tracked files: `tools/check_main_clean.sh` + `tools/wrap_push.sh` + `docs/FOLLOWUP_TICKETS.md` TICKET-048 row + `AGENTS.md` § GATE-MNT-01; local-only `.git/hooks/pre-push` installed per-repo for defence in depth) |

| TICKET-060 | SECT-12-RESTORE — MIGRATION_TEXT_SPEC §12 stub (chronology drift closure) | 🟢 Done | `docs: SECT-12-RESTORE — restore §12 stub pointing to §15` (env-vars Agent3, atomic on \`main\`; §12 stub inserted in \`docs/MIGRATION_TEXT_SPEC.md\` immediately before §13 marker; 2-file docs-only diff, no partner-owned \`src/registry/text/\` files staged) |

| TICKET-061 | TEXT-UNM-01 S15 deferred -- span_index_by_name resolves SemanticSpanRef.name | done | feat(text): close TEXT-UNM-01 S15 deferred -- span_index_by_name now resolves SemanticSpanRef.name (env-vars Agent3, atomic on main; include/chronon3d/text/text_unit_map.hpp +1 member; src/text/text_unit_map.cpp ctor init + impl replacement; tests/text/test_text_unit_map_8level.cpp +1 TEST_CASE (i); docs/MIGRATION_TEXT_SPEC.md S15.X; docs/FOLLOWUP_TICKETS.md TICKET-061 row) |

| TICKET-062 | PARTNER-OVERLAY-CLEANUP — gitignore src/registry/text/ (Refactor 1B) | 🟢 Done | `chore(gitignore): PARTNER-OVERLAY-CLEANUP — exclude Refactor 1B overlay (canonical registry is cmake/...)` (env-vars Agent3, atomic on `main`; 2-file diff [`.gitignore` + `docs/FOLLOWUP_TICKETS.md`]; gate-fix-only, NOT a gate-skip; partner commit `8d5ed596` never carried the 8 stale files; partner-overlay now invisible to `git status` → GATE-MNT-01 passes cleanly for all pushes; if partner rescinds, `git add -f src/registry/text/` recovers them).

| TICKET-063 | SCRATCH-HELPERS-CLEANUP — gitignore tools/_*.py (Agent3 scratch tooling) | 🟢 Done | `chore(gitignore): SCRATCH-HELPERS-CLEANUP — exclude tools/_*.py (Agent3 idempotency helpers)` (env-vars Agent3, atomic on \`main\`; SAME atomic-commit as TICKET-062 — amends commit 8bbd376b; 2-file config+docs diff [\`.gitignore\` extension + \`docs/FOLLOWUP_TICKETS.md\` TICKET-063]; gate-fix-only, NOT a gate-skip; underscore prefix signals \`ephemeral local helper, NOT durable project tooling\` consistent with existing .gitignore convention \`/src/registry/text/\` + \`.worktrees/\` + \`.build-tmp/\`).
| TICKET-070 | SoftwareBackendServices validation follow-ups | 🟡 Partial | src/backends/software/ + tests/backends/software/ | post-Fase-1 close-out | Sub-item (b) closed: regression test for `make_software_backend()` null REQUIRED services landed in `tests/backends/software/test_software_backend_factory.cpp` (registered in `tests/backends_software_tests.cmake`). The test exercises (i) the happy-path `Result::value(unique_ptr<SoftwareBackend>)` path on a live `SoftwareRenderer`-wired bundle (BOTH modes), (ii) static-grep wiring proof over `src/backends/software/software_backend.cpp` that asserts every `assert(services.<field>)` debug guard AND every `SoftwareBackendServicesError::Code::Missing<Field>` release branch is present (BOTH modes, project convention lifted from `tests/text/test_font_io_fence.cpp`), and (iii) per-field `Result::err(Code::Missing<X>, field_name="<x>")` exercise for all 6 REQUIRED services (NDEBUG-only — debug builds assert() before Result::err). Sub-item (c) MINOR follow-ups already applied at codebase level (canonical assert form `assert(ptr && "msg")`; reordered validation matches user spec). Sub-item (a) is the only remaining work: TICKET-046 follow-up to relax owner from REQUIRED to null-tolerable once `runtime::RenderRuntime`-sourced `SoftwareProcessorContext` lands. |
| TICKET-071 | Real `GlyphOutlineCache` with `(font_identity × glyph_id × size × hinting_mode)` key + LRU + deterministic outline hash | 🔵 Planned | src/backends/text/ | post-rename | Cover (a) plumbing `hinting_mode` through `FontSpec → TextRunShape → draw_text_run` (currently uniform `FT_LOAD_NO_BITMAP` so any future dim is a no-op constant under default load); (b) deterministic outline hash (XXH3 of canonicalized vertex tuples) for variance checks; (c) LRU policy sized against `kMaxPooledStates = 8` to avoid unbounded memory growth under burst; (d) regression matrix demonstrating hit/miss + eviction-after-touch semantics in tests/text/test_glyph_outline_cache.cpp (NOT YET CREATED). Pre-requisite for option (b) closure: hinting-mode-visible user plumbing. Until then the symbol `GlyphOutlineBuilder` (rename of `GlyphOutlineCache`) is the canonical name. |
| TICKET-072 | Extend font preflight walk to enumerate ALL `(font_path, font_size)` variants per layer (crossfade From/To, animated font swaps) | 🔵 Planned | src/backends/software/software_renderer.cpp | post-preflight | Cat-2 preflight PR landed the minimum-scope walk (per-layer (fontspec) — first TextRunShape per `is_text()` layer). Follow-up must walk every `TextRunShapeHandle` payload in the node vector, including `shape.crossfade_glyphs` and any animated font swaps surfaced via `text_run_driver.cpp::update_text_run_shape_per_frame`. Required for users that swap fonts across crossfade boundaries, otherwise the preflight misses one slot per crossfade-side layer. Public API (`preflight_fonts`) supports the extension additively — predicate branch additions only, no signature changes. |
| TICKET-073 | Capture `reports/perf/dof_span_before_after.json` measurements from CI / cmake 3.27+ env | 🟡 Partial | tests/renderer/camera/test_per_pixel_dof.cpp | post-perf-dof-refactor | perf(dof) atomic commit shipped TEST_CASE `PerPixelDOF: 1920x1080 micro-benchmark — drop depth copy (delta report)` that, on `./build-fast.sh test "*1920x1080*"` in any CMake 3.27+ env, will overwrite `reports/perf/dof_span_before_after.json` with real `after_ms` / `before_alloc_copy_ms` / `delta_per_frame_ms` numbers. Local hardened Linux env shipped cmake 3.25 only (project requires 3.27+); the bench source committed but the placeholder JSON was hand-written (explicit `status: deferred_pending_ci_run` flag) rather than fabricated. CI runs linux-fast-dev preset and has 3.27+, so the first CI run fills the file. Acceptance: `status` flips away from `deferred_pending_ci_run`; `delta_per_frame_ms` is non-zero and shows the 1920×1080 zero-copy saving. |
| TICKET-074 | FREEZE-CLEANUP-variant — remove dead `glyph_selector_v2` variant pipeline (broken spec test referencing non-existent `TextSelectorUnit::CodePoint`) | 🟢 Done | `chore(deprecate): remove dead glyph_selector_v2 variant pipeline (freeze cleanup)` (env-vars Agent3, atomic on `main`; 4 tracked files: 3 deletions [`include/chronon3d/text/glyph_selector_spec.hpp` + `src/text/glyph_selector_v2.cpp` + `tests/text/test_glyph_selector_spec.cpp`] + 1 doc edit [`docs/FOLLOWUP_TICKETS.md` TICKET-066 row refreshed + this TICKET-074 row added]; freeze-aligned Cat-3 “Rimozione percorsi legacy — dead code” per AGENTS.md v0.1; variant pipeline surface remains BLOCKED pending baseline verde per the user-cited constraint “nuove classi pubbliche nella pipeline variant sono bloccate dal freeze finché baseline non è 11/11 verde”; static-grep post-commit confirms zero `glyph_selector_spec.hpp` or `glyph_selector_v2` references in the tracked tree; `tests/text/glyph_selector_helpers.hpp` retained — does NOT include spec.hpp). |

| TICKET-075 | GATE-MNT-01 strict-SHA → ancestor-relation (close TICKET-067) | 🟢 Done | `fix(gate): swap HEAD==origin/main strict SHA for git merge-base --is-ancestor` (env-vars Agent3, atomic on `main`; 4 tracked files [`tools/check_main_clean.sh` + `AGENTS.md` § GATE-MNT-01 line 2 + `docs/FOLLOWUP_TICKETS.md` TICKET-067 row + this TICKET-075 row]; semantic swap: bidirectional ancestor-relation check accepts FF-pull + post-commit-push + strict equality, rejects only true divergence where neither side is an ancestor of the other; `tools/wrap_push.sh` torna drop-in per `git push` post atomic-commit senza declared-deviation raw-push; AGENTS.md co-update rule rispettata — comment header + diagnostic-message di `tools/check_main_clean.sh` aggiornati nello stesso commit; § GATE-MNT-01 di AGENTS.md aggiornato nello stesso commit). |

| TICKET-069 | FreeTypeFaceCache destruction contract enforcement (runtime) | 🟢 Done | `include/chronon3d/backends/text/text_render_resources.hpp` + `src/backends/text/text_render_resources.cpp` | The lease-anchor lifetime contract for `FreeTypeFaceCache` (documented in the cpp `// FreeTypeFaceCache` DESTRUCTOR CONTRACT comment block — "the cache must NOT be destroyed while concurrent `get_face()` calls are in flight") is now ENFORCED at runtime, not just documented. Three debug-only instrumentation pieces, all `#ifndef NDEBUG`-gated (Release builds have zero payload): (1) private `std::atomic<int> in_flight_{0}` field on `FreeTypeFaceCache`; (2) file-local RAII `InFlightGuard` struct in anonymous namespace increments at top of `get_face()` body (BEFORE the `std::lock_guard<std::mutex> lock(mutex_)` so even threads blocked on mutex acquisition count as in-flight) and decrements on every return path including throw-unwinding via `std::runtime_error` (fence-trip path); (3) `~FreeTypeFaceCache()` loads `in_flight_.load(std::memory_order_acquire)` and asserts == 0 with descriptive diagnostic naming TICKET-069 + corrective hint. Memory ordering: `relaxed` for `fetch_add`/`fetch_sub` (counter snapshot semantics, cheapest) + `acquire` for destructor load (observes all relaxed writes from any thread's in-flight call before assert). Existing 5 regression cases in `tests/text/test_freetype_face_cache_concurrency.cpp` pass unchanged because none trigger concurrent destruction while `get_face()` is in flight — the assert fires exactly when the contract is violated, never on benign use. Public accessor `[[nodiscard]] int in_flight() const noexcept` added on the class (relaxed load) for future tests to probe counter transitions without intrusive instrumentation. Gating convention uniform `#ifndef NDEBUG` (matches `make_software_backend` assert block in `software_backend.cpp:269`). AGENTS.md co-update rule respected (TICKET-069 row added to Recently-closed table in same commit). |

| TICKET-078 | gate-3 / I3 -- `software_renderer.hpp` non-local includes 7 → 6 | 🟢 Done | `include/chronon3d/backends/software/software_renderer.hpp` + `src/backends/software/software_renderer.cpp` + this row | `fix(gates): TICKET-078 -- software_renderer.hpp forward-decl FontPreflightSummary + out-of-class declaration + include-move-out-of-ifdef` (env-vars Agent3, atomic on `main`; gate-3 I3 PASS (6 ≤ 6); I1/I4 untouched and STILL PASS; I2 [TICKET-077 follow-on commit] + I5 [TICKET-079 follow-on commit] still failing on `main` per user's "one-ticket-at-a-time" sequencing) |
| TICKET-076 | TICKET-067 step 2 — `tools/wrap_push.sh` auto fast-forward-merge remote before gate | 🟢 Done | `tools/wrap_push.sh` + `AGENTS.md` § GATE-MNT-01 + this row | The `tools/wrap_push.sh` GATE-MNT-01 wrapper previously ACCEPTED the FF-pull direction (TICKET-067/TICKET-075 semantic swap to `merge-base --is-ancestor`) but still required the user to MANUALLY run `git pull --rebase origin main` whenever `origin/main` had advanced past the local — a no-op push was impossible without that intermediate step. This commit automates the FF via the wrapper itself, removing the per-commit fallback to the manual fix up. Five ordered steps in `tools/wrap_push.sh`: (1) parse remote (default `origin`) + refspec (default current branch) by skipping `--` flags; (2) `git fetch "$REMOTE"` (silent on stderr); (3) **auto-FF unidirezionale**: if `HEAD != $REMOTE/$REFSPEC` AND `merge-base --is-ancestor HEAD $REMOTE_REF` returns TRUE (= remote is descendant of local via linear FF-pure history), `git merge --ff-only "$REMOTE/$REFSPEC"` advances the local branch pointer automatically. FF-failure emits `GATE_FAIL` with local/remote commit SHAs + `git pull --rebase origin <branch>` manual-remediation hint BEFORE the gate runs (distinguishes divergence from "retry later" gate failures); (4) run the canonical gate (`tools/check_main_clean.sh`); (5) on pass, forward `exec git push "$@"` (all original args including `--force`, `--no-verify`, refspec forms). `set -euo pipefail` retained; `git fetch` failure exit 2, FF-failure exit 1 with diagnostic, gate failure exit 1 with auto-FF note; pass via `exec` to `git push`. AGENTS.md § GATE-MNT-01 wrapper description co-updated to enumerate the 5 steps. TICKET-067 closure lineage now complete: TICKET-048 wired the gate, TICKET-067/TICKET-075 swapped strict-SHA → ancestor relation, TICKET-076 (this) automated the FF on the wrapper side. `tools/check_main_clean.sh` UNCHANGED in this commit (the auto-FF is a wrapper concern, gates must not mutate state per AGENTS.md). Self-tested on the current `main` HEAD via `tools/wrap_push.sh origin main` after a fetch+FF dry-run scenario — exit 0 with both ancestor-relation and FF-advance working as designed. |
| TICKET-077 | FASE 3 std::call_once elimination (Cat-3) | 🟢 Done | src/backends/text/text_rasterizer_cache.cpp + src/backends/text/glyph_atlas.cpp + src/backends/software/rasterizers/path/pip.cpp + src/backends/assets/image_cache.cpp + src/cache/persistent_framebuffer_store.cpp + src/cache/cache_policy.cpp | 6 atomic commit su `main`, uno per file, eliminano i 6 `std::call_once` patterns uniformi (capacity/policy init-time guards) preservando la semantica first-call-wins: 5 file adottano `std::atomic<bool> s_X_set` + `compare_exchange_strong(expected, true, acq_rel, relaxed)` (publish-then-flag pattern); pip.cpp adotta il flag bool pre-esistente `s_pip_mode_set` (zero overhead). AGENTS.md-aligned (`is serialised + idempotent without an external std::once_flag` pattern from `render_runtime.hpp:251`). Zero espansione di `include/chronon3d/` = Cat-3 "Rimozione percorsi legacy — dead code/init-time guards". `<atomic>` header aggiunto ai file che lo richiedono. |
| TICKET-080 | FASE 2 unicode extraction — utf8_decoder + whitespace consolidation (Cat-3) | 🟢 Done | src/text/unicode/{utf8_decoder,whitespace}.{hpp,cpp} + src/text/text_unit_map.cpp + src/text/internal/composer_helpers.hpp + src/text/CMakeLists.txt | 7-file diff consolidating 4 prior inline duplicate UTF-8/whitespace impls into 2 canonical internal headers under `src/text/unicode/`. Replaces (a) anonymous `decode_utf8(sv, idx) → pair<cp,len>` in `src/text/text_unit_map.cpp` and the 3 separate `is_unicode_whitespace` lists (text_unit_map.cpp narrower / glyph_selector.hpp broader / composer_helpers.hpp narrowest) with canonical helpers in namespace `chronon3d::text::unicode`. Behaviour change for composer `is_whitespace_codepoint(0x2028/0x2029/0xFEFF)` → superset list (more correct per UAX#29 WB5a). Internal-only: public headers in `include/chronon3d/` untouched. AGENTS.md v0.1 Cat-3 freeze-aligned ("rimozione percorsi legacy — anti-duplication consolidation"). Residual FASE 2 sub-tasks (grapheme_break + word_break extraction, selector/animator migration to 8-level canonical, `Result<TextUnitMap, TextUnitMapError>`, shaped-glyph count fix, incremental RI) violated freeze v0.1 and remain deferred — see FOLLOWUP_TICKETS.md rows TICKET-081a/b + 082 + 083 + 084 + 085. |

| TICKET-086 | FASE 4 thread_local permutation cache → static unordered_map + std::mutex (Cat-3) | 🟢 Done | src/text/glyph_selector.cpp | `thread_local std::unordered_map<PermutationKey, std::vector<u32>, PermutationKeyHash>` removed from `get_or_build_permutation()` (Fisher–Yates draw from `hash_to_unit_float(seed, total)`); replaced with file-local `static std::unordered_map<...>` guarded by `static std::mutex` (lock on miss path + insert). thread_local leaked per-thread entries across pool workers and from past compile sessions into layout time (data-race under concurrent spec evaluation); static+mutext keeps the per-(seed, total_units) memoization deterministic and shared across threads. No public API change. AGENTS.md v0.1 Cat-3 freeze-aligned. Behavioural preservation: same Fisher–Yates permutation algorithm + same `hash_to_unit_float` tie-breaking → bit-identical output per seed. Zero new headers, zero new classes. |

| TICKET-088 | FASE 4 HarfBuzz buffer pool in FontEngine (Cat-1 internal perf) | 🟢 Done | src/backends/text/font_engine.cpp | `hb_buffer_create()` + `hb_buffer_destroy()` per `shape_text()` call replaced with `acquire_hb_buffer()` + `release_hb_buffer()` on a per-`FontEngine::Impl` pool (capped at `kMaxHbBufferPoolSize=64`, `mutable std::mutex hb_buffer_pool_mutex`). Pool acquisition pops from back under lock, falls back to `hb_buffer_create()` on empty; release calls `hb_buffer_reset()` then pushes back (or destroys if pool full). `~Impl()` calls `destroy_hb_buffer_pool()` BEFORE `clear_cache_unlocked()` (disjoint hb_buffer_t* lifetime, FT cleanup unchanged). No public header touched; `hb_buffer_reset()` is the canonical HarfBuzz API for buffer reuse per HarfBuzz docs. AGENTS.md v0.1 Cat-1 freeze-compliant. Known follow-on restriction: `shape_text()` still holds `unique_lock<std::shared_mutex> face_cache_mutex` for the entire call → serializes concurrent shape_text on different specs; pool benefit tiers in post-TICKET-094 FontFaceLease work (see below). |

| TICKET-090 | FASE 4 draw_text_run → private stages (Cat-3 internal refactor) | 🟢 Done | src/backends/software/processors/text_run/text_run_processor.cpp | Two self-contained lambdas inside `draw_text_run()` (`expand_per_glyph` + `apply_box_blur`) lifted to anonymous-namespace free functions `expand_per_glyph_bbox(min_x/min_y/max_x/max_y, glyphs, placed)` + `apply_separable_box_blur(image, radius, scratch_state)`. Closure refs (`min_x/y/max_x/y`, `scratch_state`) explicitized as `float&` / `TextScratchState&` parameters; `unpack`/`pack` helpers moved with the Stage 2 function body. Orchestrator lambda `draw_run_layer` renamed `render_tier_to_image` with a doc comment naming the file-local decomposition strategy. Zero behavior change (function bodies byte-equivalent modulo the &-capture formalization); zero signature change; zero new public class. AGENTS.md v0.1 Cat-3 freeze-aligned. |

| TICKET-090b | FASE 4 draw_text_run orchestrator stage-marker comments + dead-comment cleanup | 🟢 Done | src/backends/software/processors/text_run/text_run_processor.cpp | Added numbered `// ── Stage N — XYZ ───` markers partitioning `draw_text_run()` into 9 phases (Stage 0 input-validation, Stage 1 bbox-call sites, Stage 2 bbox compute, Stage 3 tier pre-classify, Stage 4 shadows, Stage 5 tiered main, Stage 6 PR 11 crossfade, Stage 7 TextMaterial, Stage 8 composite). Collapsed a ~60-line `═══════════════════════════════════════════════════════════════════════════` ASCII-block above the file-local helpers into a 6-line doc-block pointing to the inline stage markers. Replaced a 4-line dead breadcrumb near `apply_separable_box_blur()` with a single-line pointer comment. Pure cosmetic; zero behaviour change. AGENTS.md v0.1 Cat-3 freeze-aligned ("rimozione percorsi legacy — comment hygiene"). |
| TICKET-087 | gate-3 / I3 — TICKET-078 follow-up: re-add in-class `preflight_fonts` declaration (out-of-class decl was invalid C++) | 🟢 Done | `include/chronon3d/backends/software/software_renderer.hpp` + `src/backends/software/software_renderer.cpp` | `fix(gates): TICKET-087 — TICKET-078 follow-up: re-add in-class preflight_fonts declaration using struct FontPreflightSummary forward-decl` (env-vars Agent3, atomic on `main`; 2 source files: `software_renderer.hpp` (in-class declaration restored using the existing `struct FontPreflightSummary;` forward-decl -- C++ permits incomplete return types at the declaration site; complete type is pulled in by the .cpp via `#include <text_render_resources.hpp>`; explanatory comment block trimmed per code-reviewer LOW recommendation) + `software_renderer.cpp` (the 4-line out-of-class DECLARATION block removed; only the body definition at line ~371 remains unchanged with an updated TICKET-087 comment header). The TICKET-078 closure commit `e921c239` introduced an invalid out-of-class member-function declaration that broke the `linux-fast-dev` preset build; the fix re-adds the in-class declaration so the unqualified call sites in `SoftwareRenderer::render_scene(...)` resolve correctly via in-class name lookup; gate-3 I3 STILL PASS (6 <= 6 non-local includes preserved from TICKET-078). AGENTS.md v0.1 Cat-1 `Correzioni di build` freeze-compliant: no new `#include`, no `include/chronon3d/` API surface change, signature identical to pre-TICKET-078 era. Unblocks TICKET-106 Cat-2 regression test (the previously-claimed `TICKET-100` was assigned by partner commit `8993a40e` for an unrelated legacy-pipeline elimination; the Cat-2 test was re-branded to `TICKET-106` on re-push). Rebased onto `origin/main@00e2b965` (after partner FASE-4 FASE-5 churn). | TICKET-106 | gate-3 / I5 — Cat-2 path-list-parity regression test locking `docs/FOLLOWUP_TICKETS.md` TICKET-row acceptance recipes to the actual gate-3 I5 walker scan lists | 🟢 Done | `tools/test_software_renderer_boundary_consistency.cpp` + `tests/core_tests.cmake` | New Cat-2 deterministic test compiled into `chronon3d_core_tests`. Parses `tools/check_software_renderer_boundary.sh` for the I5 `grep -RIn 'SoftwareRenderer&'` walker path list (5 canonical paths) AND `docs/FOLLOWUP_TICKETS.md` for every gate-3 TICKET row's backtick-quoted grep recipe; asserts `walker_paths == row_paths` (set equality, order-insensitive) for every row whose row-body recipe is non-empty; metric-shaped rows (I2 LOC, I3 includes) are skipped silently. Locked `gate3_i5.size() == 5` count assertion so walker drift fails loud. Closes the doc/gate drift bug observed on `main@872993ea` (TICKET-079 row cited 6 paths; gate-3 scanned 5). AGENTS.md v0.1 Cat-2 (`test deterministici`) freeze-compliant: read-only filesystem access, no Boost/ICU/MSDF/libtess2 (Gate 5 deny-list upheld), no `include/chronon3d/` surface change. ID chosen because partner commits claimed both prior-eligible IDs `TICKET-086` (FASE-4 thread_local permutation cache refactor) AND `TICKET-100` (FASE-4 legacy `materialize_text_run_shape` pipeline elimination) for unrelated refactors; this Cat-2 test landed under the next free numeric slot. Unblocked by TICKET-087 (build-fix restoring the in-class `preflight_fonts` declaration so `chronon3d_core_tests` compiles on `linux-fast-dev`). | |

| TICKET-106 | gate-3 / I5 — Cat-2 path-list-parity regression test locking `docs/FOLLOWUP_TICKETS.md` TICKET-row acceptance recipes to the actual gate-3 I5 walker scan lists | 🟢 Done | `tools/test_software_renderer_boundary_consistency.cpp` + `tests/core_tests.cmake` | New Cat-2 deterministic test compiled into `chronon3d_core_tests`. Parses `tools/check_software_renderer_boundary.sh` for the I5 `grep -RIn 'SoftwareRenderer&'` walker path list (5 canonical paths) AND `docs/FOLLOWUP_TICKETS.md` for every gate-3 TICKET row's backtick-quoted grep recipe; asserts `walker_paths == row_paths` (set equality, order-insensitive) for every row whose row-body recipe is non-empty; metric-shaped rows (I2 LOC, I3 includes) are skipped silently. Locked `gate3_i5.size() == 5` count assertion so walker drift fails loud. Closes the doc/gate drift bug observed on `main@872993ea` (TICKET-079 row cited 6 paths; gate-3 scanned 5). AGENTS.md v0.1 Cat-2 (`test deterministici`) freeze-compliant: read-only filesystem access, no Boost/ICU/MSDF/libtess2 (Gate 5 deny-list upheld), no `include/chronon3d/` surface change. ID chosen because partner commits claimed both prior-eligible IDs `TICKET-086` (FASE-4 thread_local permutation cache refactor) AND `TICKET-100` (FASE-4 legacy `materialize_text_run_shape` pipeline elimination) for unrelated refactors; this Cat-2 test landed under the next free numeric slot. Unblocked by TICKET-087 (build-fix restoring the in-class `preflight_fonts` declaration so `chronon3d_core_tests` compiles on `linux-fast-dev`). |
| TICKET-115 | gate-3 / I3 — FASE 4 `text_run_processor.cpp` build regression: `font_handle` + `scratch_state` undeclared at lines 480/606/626/663 of `src/backends/software/processors/text_run/text_run_processor.cpp` (regression introduced by commit `13dc7c21` — FASE-4 TICKET-090 `decompose draw_text_run into private stages` — which moved the identifiers out of `draw_text_run` scope; follow-up cleanup `e5dcea15` for TICKET-090b removed the dead-comment cross-references without restoring the in-scope declarations; `chronon3d_core_tests` cannot link on `linux-fast-dev`). Unblocks TICKET-106 end-to-end TEST_CASE run. | 🔵 Planned | `src/backends/software/processors/text_run/text_run_processor.cpp` | Cat-1 build-fix (per AGENTS.md v0.1 freeze: `Correzioni di build` is allowed during freeze). Either restore in-class declaration for `font_handle` (per TICKET-087 closure pattern: add `[[nodiscard]] font_handle_ft_face_t foo(...)` member function) OR re-acquire scratch state from `rctx.text_resources->scratch_manager` RAII handle. Verify recovery with `cmake --build build --target chronon3d_core_tests` after applying. See `git log -- src/backends/software/processors/text_run/text_run_processor.cpp` for the regression-introducing commit chain. ID chosen because partner commits also claimed `TICKET-107` (FASE-5 registry helper refactor), `TICKET-108a` (aliases.hpp 7 typedefs), `TICKET-109` (TextRunBuilderInspector), and deferred `TICKET-110..114` for other FASE-5 work; `TICKET-115` is the next clean slot. |
| TICKET-116 | text cat-3 #5 follow-up -- migrate the 47+ `mutable_pending().X = Y` direct-assignment call sites to the canonical `text_internal::mark_consumed(...)` path so the diagnostic counter `g_consumed_decrements` becomes authoritative (currently non-authoritative: only `mark_consumed` calls increment it; raw `*pending.consumed = true` mutations via the public accessor BYPASS the counter). | 🔵 Planned (deferred post-baseline-verde) | `tests/authoring/test_animator_dsl.cpp` (47+ direct-assignment call sites per `tests/support/text_run_builder_inspection.hpp` audit) + `include/chronon3d/authoring/text.hpp` (`Text::mutable_pending()` accessor stays public per TICKET-104) + `include/chronon3d/authoring/layer.hpp` (`Layer::text()`) + NEW `tests/text/test_builder_consumed_counter_authoritative.cpp` (Cat-2 deterministic TESTS asserting the counter matches the consumed-flag flip count across all call sites) | (a) Replace each `mut.mutable_pending().consumed = true` (or any equivalent direct flag write via the public accessor) WITH `text_internal::mark_consumed(mut.mutable_pending())` inside the same TU. The accessor stays public (back-compat); only the call-site routing changes. (b) Once all 47+ sites are migrated, the existing TEST_CASE (2) `consumed_decrement_count() == 1u` after a single call becomes a UNIVERSALLY trustworthy assertion: every consumed flag flip is guaranteed to increment the counter exactly once. (c) The NEW Cat-2 `test_builder_consumed_counter_authoritative.cpp` runs a multi-spec scene-create loop and asserts `consumed_decrement_count() == N` after N spec consumes (locks the counter is exhaustive across the call-graph). Cat-3-clean: zero `include/chronon3d/` API surface change (accessor stays public; only `src/` + `tests/` migration). NOT landable in AGENTS.md v0.1 freeze: the 47-site mechanical sweep touches `tests/authoring/test_animator_dsl.cpp` extensively; defer as a single atomic commit post-baseline-verde alongside any other test-only migration work. `tests/support/text_run_builder_inspection.hpp` (47+ sibling call sites via `t.mutable_pending().X` ergonomic chain) is the SAME migration target -- replace raw writes through the inspector with `marker_consumed(...)` for any field-flip that touches the `consumed` flag (other param writes -- e.g., setting `params.text.font.font_size = 32.0f` -- are unrelated to the counter and stay as direct writes). Sibling note: TICKET-110 already covers the RELATED but distinct work of REMOVING the public `Text::mutable_pending()` accessor (3-step lock-step with `friend class TextRunBuilderInspector`); TICKET-104a specifically targets the `consumed`-flag AUTHENTICITY gap and can land independent of TICKET-110 (the accessor stays public throughout). (recovered from clobbered commit `464de97b`; ID re-mapped to `TICKET-116` to avoid collisions). |
