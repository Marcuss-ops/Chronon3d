<table>
<tr><th colspan="2">TICKET-128 — Test 18 Weekly founder dashboard (open, P1 active)</td></td></tr>
<tr><th>Stato</th><td>OPEN (Test 18 weekly founder dashboard aggregator + template landed 2026-07-12; aggregator script <code>tools/run_weekly_scorecard.sh</code> + dashboard template <code>docs/product-tests/TEST-18-WEEKLY-DASHBOARD.md</code>)</td></tr>
<tr><th>Priorità</th><td>P1 (active; founder weekly cadence starts at next Friday)</td></tr>
<tr><th>Problema</th><td>Test 18 (Long-form content post-M1.8 surface) non ha ancora uno scope canonico definito. TICKET-125 row 18 carries NOT-YET-OPENED marker onesto + this stub ticket. Differenzia da Test 17 (Confronto diretto) per la timeline-axis: Test 17 = one-shot benchmark 8 metriche × 3 prodotti; Test 18 = long-form content focus (probabilmente ≥10min video rendering surface + kinetic typography + multi-scene composition + audio STAGED per §honesty); scope TBD.</td></tr>
<tr><th>Evidenza</th><td>TICKET-125 row 18 (NOT-YET-OPENED marker onesto). The M1.8 Text Simplicity plan (`docs/TEXT_SIMPLICITY_ACTION_PLAN.md`) + Test 15 (Test del prodotto — non-developer evidence) + Test 17 (Confronto diretto) collectively surface the long-form content axis but no specific Test 18 spec exists in AGENTS.md at HEAD. Forward-point post-F2-RESOLVED.</td></tr>
<tr><th>Impatto</th><td>Senza uno scope canonico Test 18, l'aggregator TICKET-125 11-row index è structurally incomplete (1 row carries NOT-YET-OPENED marker). Dopo F2-RESOLVED + post-M1.8 close, Test 18 scope becomes plannable: long-form video rendering surface + multi-scene composition + audio STAGED per §honesty (no `scene.audio()` API surface exists).</td></tr>
<tr><th>Confine</th><td>Pure <code>docs/</code> artifact (no SDK API surface; no <code>include/chronon3d/</code> edits per Cat-3). Scope + deliverable definition deferred to post-F2-RESOLVED cycle per AGENTS.md §"Fare PR piccole e mirate".</td></tr>
<tr><th>Soluzione accettabile</th><td><strong>SPEC CANONICAL (Test 18 = weekly founder dashboard)</strong> per user spec 2026-07-12. <strong>8 metrics</strong>: (1) <code>videos_completed</code> count over 7d window; (2) <code>failure_rate</code> FAILED/total over 7d; (3) <code>manual_touches_per_video</code> avg over 7d (Test 8 wire-up); (4) <code>cost_per_finished_minute</code> requires <code>WEEKLY_COST_HOURLY_RATE</code> env var (no hardcoded fallback per §honesty); (5) <code>p95_render_time</code> percentile 95 of render_ms over 7d; (6) <code>peak_memory</code> MAX(framebuffer_bytes_peak) over 7d; (7) <code>deterministic_hash_failures</code> GATE_FAIL count from selftest logs; (8) <code>bbox_contract_violations</code> SUM from FU01 counter over 7d. <strong>2 artifacts</strong>: NEW aggregator <code>tools/run_weekly_scorecard.sh</code> (~140 LoC bash+sqlite3+awk+date, exits 0 on populated telemetry SQLite / 2 INTERNAL on missing tools or DB) + NEW dashboard template <code>docs/product-tests/TEST-18-WEEKLY-DASHBOARD.md</code> (~110 LoC with sample weekly entry, 7 narrative lines, §honesty cert). <strong>Cadence</strong>: weekly (founder runs aggregator on Friday afternoon). <strong>Per Cat-3 zero new SDK API</strong>: aggregator queries existing telemetry surfaces (SQLite + JSONL + selftest logs) — no new public header, no new registry, no new singleton, no new cat-1 public symbol. <strong>Per Cat-4 ancillary sibling-gate pattern</strong>: aggregator is a <code>tools/</code> artifact (mirrors <code>tools/check_push_divergence_window.sh</code> + <code>tools/audit_incomplete_type_pattern.sh</code> precedent); not a pre-push gate (deferred to forward-point).</td></tr>
<tr><th>Criteri di accettazione</th><td>(1) <code>tools/run_weekly_scorecard.sh</code> exists + is executable + bash syntax-check PASS (<code>bash -n</code>); (2) <code>docs/product-tests/TEST-18-WEEKLY-DASHBOARD.md</code> exists with sample weekly entry + 7 narrative lines + §honesty cert + cross-link; (3) TICKET-125 row 18 refactored from <code>NOT-YET-OPENED</code> to <code>OPEN (Weekly founder dashboard)</code> with cross-link to dashboard file; (4) this stub promoted from <code>NOT-YET-OPENED</code> to <code>OPEN</code> per the canonical user spec verbatim; (5) Cat-5 4-doc aligned (CHANGELOG prepended + FOLLOWUP_TICKETS row promoted to P1 OPEN + CURRENT_STATUS cite-only row per L3 precedent + TICKET-125 row update); (6) per-row PASS criterion eseguibile come <code>bash tools/run_weekly_scorecard.sh &gt;&gt; /tmp/weekly.md</code> (exit 0 on populated telemetry SQLite per AGENTS.md §honesty).</td></tr>
</table>

# Cross-link

- TICKET-125 row 18 (NOT-YET-OPENED marker onesto)
- `docs/TEXT_SIMPLICITY_ACTION_PLAN.md` (M1.8 — Text Simplicity plan)
- Test 15 HARNESS-COMPLETE parallel (4 docs landed commit `16855f33`)
- Test 17 TABLE-COMPLETE row (the 24-cell Confronto diretto; Test 18 = long-form-axis extension)
- AGENTS.md §honesty + §"Fare PR piccole e mirate" + §Cat-3 audio STAGED placeholder pattern

# §honesty dynamic cert

- Aggregator script queries OBSERVABLE telemetry surfaces; macchina-verifica of the aggregator on this VPS is PARTIAL per AGENTS.md §honesty (telemetry SQLite may be empty on this VPS — the script returns GATE_FAIL_INTERNAL exit 2 on missing DB; <code>bash -n</code> syntax-check PASS per L1 lint discipline).
- <code>WEEKLY_COST_HOURLY_RATE</code> env var is REQUIRED for metric 4 (cost_per_finished_minute) per §honesty "non inventare"; unset = <code>[UNSET-rate]</code> placeholder, NEVER a fabricated spot rate.
- First-run caveat: the founder MUST run the aggregator at least once to seed the SQLite cache; the sample entries + sample tables in the dashboard template are FILL-IN-THE-BLANK commitments, not synthetic real-data.
- macchina-verifica of the 8 metrics end-to-end with a populated DB is DEFERRED to a working build host per AGENTS.md §honesty (this VPS lacks chronon3d_cli binary + populated telemetry).
- Per AGENTS.md §"Regole di lint documentale" the aggregator emits one <code>[INFO] ${GATE_NAME}: ...</code> line on run completion (PASS addizionale al output markdown).
