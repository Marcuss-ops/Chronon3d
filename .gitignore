# ── Test fixtures (bootstrap-only policy) ────────────────────────────────────
# Everything under assets/ is checksum-pinned by tools/bootstrap_test_fonts.py
# (blob SHA-1, snapshot a31b1627). Fixture binaries are intentionally untracked:
# the script materializes them network-free from the local git object store,
# falling back to the pinned GitHub snapshot. Media patterns below must not
# re-expose them, hence assets/ is ignored wholesale and never negated.
assets/

# ── IDE / Editor ───────────────────────────────────────────────────────────────
.idea/
.vscode/
.vs/
*.swp
*.swo
*~

# ── CMake / build output ───────────────────────────────────────────────────────
build/
build-*/
cmake-build-*/
out/
dist/
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
CTestTestfile.cmake
CMakeDoxyfile.in
CMakeDoxygenDefaults.cmake
Testing/

# ── Binaries / libraries ──────────────────────────────────────────────────────
*.o
*.obj
*.lo
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll
*.dll.a
*.exe
*.out
*.app
*.pdb
*.idb
*.ilk

# ── C++ coverage ───────────────────────────────────────────────────────────────
*.gcda
*.gcno
*.gcov
coverage/
lcov.info

# ── clang / tooling ────────────────────────────────────────────────────────────
compile_commands.json
.clangd/
.cache/
.mlcache/

# ── Python (scripts / tests) ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# ── OS junk ────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Media / render output ──────────────────────────────────────────────────────
*.mp4
*.mov
*.avi
*.mkv
*.webm
*.gif
*.png
*.jpg
*.jpeg
*.bmp
*.tga
*.exr
!tests/**
!docs/**

renders/
render_output/
frames/
output/
outputs/
screenshots/
artifacts/
test_outputs/

# ── Logs & profiling ────────────────────────────────────────────────────────────
*.log
*.trace
*.profraw
*.profdata
*.perf
*.flamegraph
*.speedscope.json
# Zero-copy gate / pipeline-stage timing reports (generated per run).
*.timing.json
/--hot-path*
hot-path/
profile-results/

# ── Temporary / generated ──────────────────────────────────────────────────────
tmp/
temp/
.tmp/
*.tmp
*.bak
*.orig
*.rej
generated/

# Local libabigail XML baseline. CI rebuilds the pinned baseline commit instead.
tools/sdk/chronon3d_c.abi

# ── vcpkg / third-party ────────────────────────────────────────────────────────
vcpkg_installed/
downloads/
tools/vcpkg/
third_party/build/
third_party/install/

# ── Bazel (if used by tooling) ─────────────────────────────────────────────────
bazel-*

# ── Local build caches / artifacts ──
.ccache/
vcpkg_bootstrap
test_renders/
reports/
# NOTE: bench/baselines/ is NOT ignored — official benchmark baselines
# (bench/baselines/main-<sha>-bench.json) are committed per docs/CHRONON_PLAN.md
# Fase 0. Raw benchmark run output stays out of the repo:
bench/results/
bench/raw/
